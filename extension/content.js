(function(){const JOB_KEY="kintaiJob";const PENDING_KEY="kintaiPendingBulk";const SECTION_PREFS_KEY="kintaiSectionPrefs";const AUTOFILL_PREFS_KEY="kintaiAutoFillPrefs";const SECTION_DEFS=[{key:"knm",label:"勤怠　実績"},{key:"home",label:"在宅勤務/終日外出実績"},{key:"projects",label:"工数　実績"},{key:"flexRest",label:"フレ有休などの30分単位休暇"},{key:"restCorrection",label:"休憩補正"},{key:"outing",label:"私用外出/リフレッシュタイム/午後出社時の休憩"},{key:"privateCar",label:"私有車利用実績"},{key:"report",label:"報告欄"}];function sleep(ms){return new Promise(resolve=>setTimeout(resolve,ms))}async function waitFor(condFn,timeout=8e3,interval=200){const start=Date.now();while(Date.now()-start<timeout){const result=condFn();if(result)return result;await sleep(interval)}return null}function callPageFn(action,args=[],timeoutMs=6e3){return new Promise((resolve,reject)=>{const id=Math.random().toString(36).slice(2);let settled=false;function onDone(e){if(!e.detail||e.detail.id!==id||settled)return;settled=true;document.removeEventListener("kintai-helper-done",onDone);if(e.detail.ok)resolve();else reject(new Error(e.detail.error||"page function failed"))}document.addEventListener("kintai-helper-done",onDone);document.dispatchEvent(new CustomEvent("kintai-helper-exec",{detail:{id:id,action:action,args:args}}));setTimeout(()=>{if(settled)return;settled=true;document.removeEventListener("kintai-helper-done",onDone);reject(new Error(`${action} の応答がタイムアウトしました`))},timeoutMs)})}async function getJob(){const data=await chrome.storage.local.get(JOB_KEY);return data[JOB_KEY]||null}async function setJob(job){try{sessionStorage.setItem("kintaiJobActive",job&&job.active?"1":"0")}catch(e){}await chrome.storage.local.set({[JOB_KEY]:job})}async function appendLog(job,message){job.log=job.log||[];job.log.push({time:(new Date).toLocaleTimeString("ja-JP"),message:message});if(job.log.length>300)job.log.splice(0,job.log.length-300);job.lastActivityAt=Date.now();await setJob(job)}function isReauthVisible(){const el=document.getElementById("reauthentication");return!!(el&&el.style.display!=="none")}function neutralizePasswordFields(){document.querySelectorAll('input[type="password"]').forEach(el=>{el.setAttribute("type","text")})}const SCHEMA_VERSION=5;async function ensureFreshSchema(){const data=await chrome.storage.local.get("kintaiSchemaVersion");if(data.kintaiSchemaVersion===SCHEMA_VERSION)return;const all=await chrome.storage.local.get(null);const staleKeys=Object.keys(all).filter(k=>k.startsWith("kintai")&&k!=="kintaiSchemaVersion"&&k!==SECTION_PREFS_KEY&&k!==AUTOFILL_PREFS_KEY);if(staleKeys.length)await chrome.storage.local.remove(staleKeys);await chrome.storage.local.set({kintaiSchemaVersion:SCHEMA_VERSION})}function detectPageType(){if(document.getElementById("APPROVALGRD"))return"list";if(document.getElementById("PM_TAB_WIDGET"))return"detail";if(document.forms["FormConfirmSingleDailySubmission"])return"confirm";return"other"}function findMenuRecoveryLink(){const norm=s=>s.replace(/[\s\u3000]/g,"");const links=Array.from(document.querySelectorAll("a"));return links.find(a=>norm(a.textContent)==="勤怠")||links.find(a=>norm(a.textContent)==="勤怠実績入力")||null}function clickDetailButton(dateKey){const btn=document.getElementById(`BTNDTL${dateKey}0`);if(!btn)return false;btn.click();return true}function parseHeaderDate(){const hd=document.querySelector("#HD span");if(!hd)return null;const m=hd.textContent.match(/(\d+)年(\d+)月(\d+)日/);if(!m)return null;return{year:Number(m[1]),month:Number(m[2]),day:Number(m[3])}}function getDetailDateKey(){const d=parseHeaderDate();return d?`${d.year}_${d.month}_${d.day}`:null}function getDisplayedYearMonthDay(){return parseHeaderDate()}function daysInMonth(year,month){return new Date(year,month,0).getDate()}function isMonthStarted(year,month){const now=new Date;const nowKey=now.getFullYear()*12+now.getMonth();const targetKey=year*12+(month-1);return targetKey<=nowKey}function calcEquinoxDay(year,isSpring){const base=isSpring?20.8431:23.2488;return Math.floor(base+.242194*(year-1980)-Math.floor((year-1980)/4))}function nthMondayOfMonth(year,month,n){let count=0;const total=daysInMonth(year,month);for(let day=1;day<=total;day++){if(new Date(year,month-1,day).getDay()===1){count++;if(count===n)return day}}return null}function getFixedHolidays(year){const map={};map["1-1"]="元日";map[`1-${nthMondayOfMonth(year,1,2)}`]="成人の日";map["2-11"]="建国記念の日";if(year>=2020)map["2-23"]="天皇誕生日";map[`3-${calcEquinoxDay(year,true)}`]="春分の日";map["4-29"]="昭和の日";map["5-3"]="憲法記念日";map["5-4"]="みどりの日";map["5-5"]="こどもの日";map[`7-${nthMondayOfMonth(year,7,3)}`]="海の日";map["8-11"]="山の日";map[`9-${nthMondayOfMonth(year,9,3)}`]="敬老の日";map[`9-${calcEquinoxDay(year,false)}`]="秋分の日";map[`10-${nthMondayOfMonth(year,10,2)}`]="スポーツの日";map["11-3"]="文化の日";map["11-23"]="勤労感謝の日";return map}function isJapaneseHoliday(year,month,day){const map=getFixedHolidays(year);if(map[`${month}-${day}`])return true;const date=new Date(year,month-1,day);if(date.getDay()!==0){const prev=new Date(year,month-1,day-1);if(prev.getDay()===0&&map[`${prev.getMonth()+1}-${prev.getDate()}`])return true}return false}function buildMonthDays(year,month){const count=daysInMonth(year,month);const weekdayNames=["日","月","火","水","木","金","土"];const days=[];for(let d=1;d<=count;d++){const wdIdx=new Date(year,month-1,d).getDay();const isHoliday=wdIdx===0||wdIdx===6||isJapaneseHoliday(year,month,d);days.push({key:`${year}_${month}_${d}`,day:d,weekday:weekdayNames[wdIdx],isHoliday:isHoliday})}return days}function monthStatusKey(year,month){return`kintaiMonthStatus_${year}_${month}`}function getDisplayedListYearMonth(){const grid=document.getElementById("APPROVALGRD");if(!grid)return null;const cb=grid.querySelector('input[type="checkbox"][id^="CHK"]');if(!cb)return null;const m=cb.id.match(/^CHK(\d+)_(\d+)_(\d+)$/);if(!m)return null;return{year:Number(m[1]),month:Number(m[2])}}async function scrapeAndCacheMonthStatus(){const grid=document.getElementById("APPROVALGRD");if(!grid)return;const statusByYearMonth={};grid.querySelectorAll('input[type="checkbox"][id^="CHK"]').forEach(cb=>{const m=cb.id.match(/^CHK(\d+)_(\d+)_(\d+)$/);if(!m)return;const[,year,month,day]=m;const tr=cb.closest("tr");if(!tr)return;const hasError=!!tr.querySelector(".error");const errorImg=tr.querySelector(".error img");const errorText=errorImg?errorImg.getAttribute("alt")||"":"";const text=tr.textContent;const isSubmitted=/申請中|確認済|差戻し|却下/.test(text);const isSeriousError=/下回って|超過|不整合|未入力/.test(errorText);let status="blank";if(isSeriousError)status="error";else if(isSubmitted)status="done";else if(hasError)status="error";const kcdEl=tr.querySelector('[id^="KCD"]');const kcdColor=kcdEl?kcdEl.querySelector("font")?.getAttribute("color"):null;const isHolidayLabel=kcdColor==="FF0000";const kcdText=kcdEl?kcdEl.textContent.trim():"";const isLeaveLabel=/休暇|他休/.test(kcdText);const isApplied=/申請中|確認済/.test(text);const tds=tr.querySelectorAll(":scope > td");const commuteData=parseObjectiveDataCell(tds[5]);const pcData=parseObjectiveDataCell(tds[6]);const timeRange=computeEffectiveTimeRange(pcData,commuteData);const dateKey=`${year}_${month}_${day}`;const workedHEl=tr.querySelector(`[id^="RTTL105@${dateKey}0_"][id$="H"]`);const workedMEl=tr.querySelector(`[id^="RTTL105@${dateKey}0_"][id$="M"]`);const execMinutes=workedHEl&&workedMEl?Number(workedHEl.textContent)*60+Number(workedMEl.textContent):null;const reportText=tds[23]?tds[23].textContent.replace(/\u00a0/g,"").trim():"";const hasReportComment=reportText!=="";const discrepancyText=tds[7]?tds[7].textContent.replace(/\u00a0/g,"").trim():"";const hasDiscrepancy40=discrepancyText==="40"||discrepancyText.includes("その他");const hasDiscrepancyReason=discrepancyText!=="";const execSTHEl=document.getElementById(`K${dateKey}0STH`);const execSTMEl=document.getElementById(`K${dateKey}0STM`);const execETHEl=document.getElementById(`K${dateKey}0ETH`);const execETMEl=document.getElementById(`K${dateKey}0ETM`);let hasTimeDeviation=false;if(execSTHEl&&execSTMEl&&execETHEl&&execETMEl&&pcData){const execStartMin=Number(execSTHEl.textContent)*60+Number(execSTMEl.textContent);const execEndMin=Number(execETHEl.textContent)*60+Number(execETMEl.textContent);const pcMin=rangeToMinutes(pcData);const startDeviates=Math.abs(execStartMin-pcMin.start)>=30;const endDeviates=Math.abs(execEndMin-pcMin.end)>=30;hasTimeDeviation=startDeviates||endDeviates}if(hasTimeDeviation&&tds[4])tds[4].style.backgroundColor="#ffff00";const ymKey=`${year}_${month}`;statusByYearMonth[ymKey]=statusByYearMonth[ymKey]||{};statusByYearMonth[ymKey][day]={status:status,isHoliday:isHolidayLabel,isLeave:isLeaveLabel,applied:isApplied,timeRange:timeRange,commuteData:commuteData,pcData:pcData,execMinutes:execMinutes,hasReportComment:hasReportComment,hasDiscrepancy40:hasDiscrepancy40,hasDiscrepancyReason:hasDiscrepancyReason,hasTimeDeviation:hasTimeDeviation}});const sets={};Object.entries(statusByYearMonth).forEach(([ymKey,byDay])=>{const[year,month]=ymKey.split("_");sets[monthStatusKey(year,month)]=byDay});if(Object.keys(sets).length>0)await chrome.storage.local.set(sets);ensureTimeDeviationLegendNoteInjected()}function ensureTimeDeviationLegendNoteInjected(){if(document.querySelector(".kh-time-deviation-legend-item"))return;const anchor=document.querySelector(".mg_dc_lockcancel");if(!anchor)return;let node=anchor.nextSibling;while(node&&node.nodeName!=="BR")node=node.nextSibling;if(!node||!node.parentNode)return;const item=document.createElement("span");item.className="kh-time-deviation-legend-item";item.innerHTML='客観差異：[<span style="background-color:#ffff00;">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</span>]';node.parentNode.insertBefore(item,node)}async function getCachedMonthStatus(year,month){const key=monthStatusKey(year,month);const data=await chrome.storage.local.get(key);return data[key]||{}}function parseObjectiveDataCell(td){if(!td)return null;const hSpans=td.querySelectorAll('span[id="H"]');const mSpans=td.querySelectorAll('span[id="M"]');if(hSpans.length<2||mSpans.length<2)return null;return{stH:Number(hSpans[0].textContent),stM:Number(mSpans[0].textContent),etH:Number(hSpans[1].textContent),etM:Number(mSpans[1].textContent)}}function floorTo10(h,m){const floored=Math.floor((h*60+m)/10)*10;return{h:Math.floor(floored/60),m:floored%60}}function rangeToMinutes(range){return{start:range.stH*60+range.stM,end:range.etH*60+range.etM}}function computeEffectiveTimeRange(pcData,commuteData){const src=pcData||commuteData||{stH:9,stM:0,etH:17,etM:30};const st=floorTo10(src.stH,src.stM);const et=floorTo10(src.etH,src.etM);return{stH:st.h,stM:st.m,etH:et.h,etM:et.m}}function buildWarningOverlay({baseId:baseId,zIndex:zIndex,color:color,title:title,message:message,applyLabel:applyLabel,onApply:onApply}){const overlay=document.createElement("div");overlay.id=`${baseId}-overlay`;overlay.style.cssText=`position:fixed;inset:0;z-index:${zIndex};background:rgba(0,0,0,0.85);display:flex;align-items:center;justify-content:center;`;const applyBtnHtml=applyLabel?`<button id="${baseId}-apply" style="background:${color};color:#fff;border:none;border-radius:4px;padding:10px 22px;font-weight:bold;font-size:16px;cursor:pointer;margin-right:8px;">${applyLabel}</button>`:"";overlay.innerHTML=`\n      <div style="background:#fff;border:4px solid ${color};border-radius:10px;padding:20px 28px;font-size:18px;max-width:420px;min-width:280px;box-shadow:0 6px 24px rgba(0,0,0,0.4);color:#222;text-align:center;">\n        <div style="font-size:22px;font-weight:bold;color:${color};margin-bottom:10px;">${title}</div>\n        <div style="margin-bottom:16px;text-align:left;">${message}</div>\n        ${applyBtnHtml}<button id="${baseId}-close" style="background:#6633FF;color:#fff;border:none;border-radius:4px;padding:10px 22px;font-weight:bold;font-size:16px;cursor:pointer;">閉じる</button>\n      </div>\n    `;overlay.querySelector(`#${baseId}-close`).addEventListener("click",()=>overlay.remove());if(applyLabel){overlay.querySelector(`#${baseId}-apply`).addEventListener("click",async()=>{overlay.remove();await onApply()})}return overlay}function buildLeaveWarningOverlay(year,month,days){return buildWarningOverlay({baseId:"kh-leave-warning",zIndex:2147483646,color:"#e6a817",title:"⚠ 休暇の申請忘れにご注意",message:`未申請の休暇が${days.length}件あります。月末までに申請してください。`,applyLabel:"申請する",onApply:()=>startLeaveApplyJob(year,month,days)})}async function startLeaveApplyJob(year,month,days){const job={active:true,kind:"leaveApply",queue:days.map(d=>`${year}_${month}_${d}`),currentIndex:0,log:[],startedAt:Date.now()};await setJob(job);await appendLog(job,`=== 未申請の休暇${job.queue.length}件の申請を開始します ===`);await updateStatusPanel();await processIfJobActive();await updateStatusPanel()}async function maybeShowDayWarning(filterFn,buildFn){const displayed=getDisplayedListYearMonth();if(!displayed)return;const{year:year,month:month}=displayed;if(!isMonthStarted(year,month))return;const monthStatus=await getCachedMonthStatus(year,month);const targetDays=Object.entries(monthStatus).filter(([,info])=>filterFn(info)).map(([day])=>Number(day)).sort((a,b)=>a-b);if(targetDays.length===0)return;document.body.appendChild(buildFn(year,month,targetDays))}async function maybeShowUnappliedLeaveWarning(){await maybeShowDayWarning(info=>info.isLeave&&!info.applied,buildLeaveWarningOverlay)}function buildWorkdayWarningOverlay(year,month,days){return buildWarningOverlay({baseId:"kh-workday-warning",zIndex:2147483645,color:"#2f6fed",title:"⚠ 出勤日の申請忘れにご注意",message:`未申請の出勤日が${days.length}件あります。申請してください。`,applyLabel:"申請する",onApply:()=>startWorkdayApplyJob(year,month,days)})}async function startWorkdayApplyJob(year,month,days){const job={active:true,kind:"workdayApply",queue:days.map(d=>`${year}_${month}_${d}`),currentIndex:0,log:[],startedAt:Date.now()};await setJob(job);await appendLog(job,`=== 未申請の出勤日${job.queue.length}件の申請を開始します ===`);await updateStatusPanel();await processIfJobActive();await updateStatusPanel()}async function maybeShowUnappliedWorkdayWarning(){await maybeShowDayWarning(info=>!info.isHoliday&&!info.isLeave&&!info.applied,buildWorkdayWarningOverlay)}function buildLongExecWarningOverlay(year,month,days){return buildWarningOverlay({baseId:"kh-longexec-warning",zIndex:2147483644,color:"#d9534f",title:"⚠ 報告欄の記入漏れにご注意",message:`実労働時間が13.5時間を超えている(半徹)のに報告欄にコメントが記載されていない日が${days.length}件あります（${days.join("日、")}日）。`})}async function maybeShowLongExecWithoutReportWarning(){await maybeShowDayWarning(info=>info.execMinutes!==null&&info.execMinutes!==undefined&&info.execMinutes>13.5*60&&!info.hasReportComment,buildLongExecWarningOverlay)}function buildDiscrepancy40WarningOverlay(year,month,days){return buildWarningOverlay({baseId:"kh-discrepancy40-warning",zIndex:2147483643,color:"#e67e22",title:"⚠ 報告欄の記入漏れにご注意",message:`客観差異理由CDが「40（その他）」なのに報告欄にコメントが記載されていない日が${days.length}件あります（${days.join("日、")}日）。`})}async function maybeShowDiscrepancy40WithoutReportWarning(){await maybeShowDayWarning(info=>info.hasDiscrepancy40&&!info.hasReportComment,buildDiscrepancy40WarningOverlay)}function buildTimeDeviationWarningOverlay(year,month,days){return buildWarningOverlay({baseId:"kh-timedeviation-warning",zIndex:2147483642,color:"#9b59b6",title:"⚠ 実行時間の乖離にご注意",message:`実行時間の開始・終了がPC客観データと30分以上乖離している日が${days.length}件あります（${days.join("日、")}日）。`})}async function maybeShowTimeDeviationWarning(){await maybeShowDayWarning(info=>info.hasTimeDeviation&&!info.hasDiscrepancyReason,buildTimeDeviationWarningOverlay)}const HOME_WORK_TYPE_OPTIONS=[["","更新しない"],["1","在宅勤務なし"],["3","終日在宅（通勤出社なし）"],["2","一部在宅（通勤出社あり）"],["4","一部在宅（通勤出社なし）"],["5","終日外出（通勤出社なし）"],["auto","自動判別"]];function buildAutoFillOverlay(year,month,monthStatus,savedPrefs,restoreDays){ensureBulkStylesInjected();const overlay=document.createElement("div");overlay.id="kh-autofill-overlay";overlay.style.cssText="position:fixed;inset:0;z-index:2147483647;background:rgba(0,0,0,0.85);display:flex;align-items:center;justify-content:center;";const panel=document.createElement("div");panel.style.cssText="background:#fff;border:4px solid #6633FF;border-radius:10px;padding:24px 28px;max-width:680px;width:92%;max-height:85vh;overflow:auto;color:#222;box-shadow:0 6px 24px rgba(0,0,0,0.4);line-height:1.6;font-size:15px;";panel.innerHTML=`\n      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:16px;">\n        <div style="font-size:22px;font-weight:bold;color:#6633FF;">一括入力</div>\n        <button type="button" id="kh-autofill-help-btn" style="background:#f0edff;color:#6633FF;border:1px solid #6633FF;border-radius:4px;padding:8px 16px;font-weight:bold;cursor:pointer;font-size:16px;">ヘルプ</button>\n      </div>\n      <div id="kh-autofill-help-panel" style="display:none;background:#f8f6ff;border:1px solid #6633FF;border-radius:6px;padding:14px 16px;font-size:14px;line-height:1.9;margin-bottom:18px;">\n        <ul style="margin:0;padding-left:1.2em;list-style-type:disc;list-style-position:outside;">\n          <li style="margin-bottom:6px;list-style-type:disc;">カレンダーで自動申請したい日を選択します（複数選択可）。</li>\n          <li style="margin-bottom:6px;list-style-type:disc;">在宅/外出実績は「在宅勤務/終日外出実績」の区分です。\n            <ul style="margin:6px 0 0;padding-left:1.2em;list-style-type:disc;list-style-position:outside;">\n              <li style="margin-bottom:4px;list-style-type:disc;">「更新しない」: 元の値のまま変更しません。</li>\n              <li style="margin-bottom:4px;list-style-type:disc;">「在宅勤務なし」「終日外出」「終日在宅」: 在宅勤務時間帯を0:00に設定します。</li>\n              <li style="margin-bottom:4px;list-style-type:disc;">「一部在宅（通勤出社あり）」: 時間帯欄が未入力の場合、実行時間と入退館客観データの差分（重ならない部分）から10分単位で自動算出します。</li>\n              <li style="margin-bottom:4px;list-style-type:disc;">「一部在宅（通勤出社なし）」: 自動算出できないため、時間帯欄が未入力のまま登録しようとするとエラーになります。</li>\n              <li style="list-style-type:disc;">「自動判別」: 日ごとにPC客観データ・入退館客観データの有無と差分から、以下のルールで実施区分を自動選択します。\n                <ul style="margin:6px 0 0;padding-left:1.2em;list-style-type:disc;list-style-position:outside;">\n                  <li style="margin-bottom:4px;list-style-type:disc;">PC客観データ・入退館客観データがともに「なし」→ 在宅勤務なし</li>\n                  <li style="margin-bottom:4px;list-style-type:disc;">PC客観データのみ「あり」→ 終日在宅（通勤出社なし）</li>\n                  <li style="margin-bottom:4px;list-style-type:disc;">両方「あり」で時間の差分が30分未満 → 在宅勤務なし</li>\n                  <li style="list-style-type:disc;">両方「あり」で時間の差分が30分以上 → 一部在宅（通勤出社あり）（在宅勤務時間帯も自動算出）</li>\n                </ul>\n              </li>\n            </ul>\n          </li>\n          <li style="margin-bottom:6px;list-style-type:disc;">登録番号ごとに使用時間を入力できます。空欄のままにすると、実働時間から他の登録番号の使用時間を差し引いた残りを、空欄の登録番号の数で10分単位に均等に割って自動入力します（端数は最後の1件にまとめます）。登録番号を1件も入力しなければ工数の登録は行わず、他の項目のみ設定します。</li>\n          <li style="margin-bottom:6px;list-style-type:disc;">実行時間は、未入力の場合は月一覧のPC客観データ→（なければ）入退館客観データ→（どちらもなければ）9:00～17:30の優先順位で決定し、10分単位で切り捨てて入力します。入力した場合（時:分、未入力は0として扱います）は選択した全ての日にその時間を使用します。開始・終了の日付（前日/当日/翌日）は時間入力欄の左のプルダウンで選択できます（初期値はどちらも「当日」）。終了日が開始日より過去になっている場合は「一括登録」実行時にエラーになります。</li>\n          <li style="margin-bottom:6px;list-style-type:disc;">既にその日に登録済みの工数がある場合は、いったん全て削除してから入力し直します。</li>\n          <li style="margin-bottom:6px;list-style-type:disc;">既に「申請中」の日を選んでも、内容を上書きして再度申請します。</li>\n          <li style="margin-bottom:6px;list-style-type:disc;">指定済みの使用時間の合計が実働時間と一致しない場合も登録は試み、ログに警告を残した上でCWS側の判定に委ねます（CWSが受け付けない場合はこの日をスキップします）。</li>\n          <li style="margin-bottom:6px;list-style-type:disc;">処理中は「処理を中断する」ボタンでいつでも中断できます。</li>\n          <li style="list-style-type:disc;">まだ開始していない未来の月（例: 今が9月なら10月以降）はCWSが登録を受け付けないため使用できません。</li>\n        </ul>\n      </div>\n      <div class="kh-cal-legend" style="margin-bottom:8px;">\n        <span class="kh-legend-dot" style="background:#fff"></span>未入力\n        <span class="kh-legend-dot" style="background:#ffd6d6"></span>エラーあり\n        <span class="kh-legend-dot" style="background:#dff5df"></span>入力済み\n        <span class="kh-legend-dot" style="background:#cfe2ff"></span>休暇\n      </div>\n      <div id="kh-autofill-cal-grid" class="kh-cal-grid" style="margin-bottom:10px;"></div>\n      <div class="kh-cal-actions" style="display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin-bottom:20px;">\n        <button type="button" id="kh-autofill-weekday-select" style="font-size:15px;padding:6px 14px;">全選択（平日）</button>\n        <button type="button" id="kh-autofill-workday-select" style="font-size:15px;padding:6px 14px;">全選択（勤務日）</button>\n        <button type="button" id="kh-autofill-error-select" style="font-size:15px;padding:6px 14px;">全選択（エラー日）</button>\n        <button type="button" id="kh-autofill-clear" style="font-size:15px;padding:6px 14px;">全選択解除</button>\n        <span id="kh-autofill-count">0件選択中</span>\n      </div>\n      <hr style="margin:16px 0;">\n      <div style="margin-bottom:16px;">\n        <label style="font-weight:bold;">実行時間（空欄の場合はPC客観データ等から自動算出）:</label>\n        <div style="margin-top:8px;">\n          <select id="kh-autofill-time-std" style="padding:4px 6px;"><option value="-1">前日</option><option value="0" selected>当日</option><option value="1">翌日</option></select>\n          <input type="text" id="kh-autofill-time-sth" style="width:32px;padding:5px 6px;text-align:center;">\n          :\n          <input type="text" id="kh-autofill-time-stm" style="width:32px;padding:5px 6px;text-align:center;">\n          ～\n          <select id="kh-autofill-time-etd" style="padding:4px 6px;"><option value="-1">前日</option><option value="0" selected>当日</option><option value="1">翌日</option></select>\n          <input type="text" id="kh-autofill-time-eth" style="width:32px;padding:5px 6px;text-align:center;">\n          :\n          <input type="text" id="kh-autofill-time-etm" style="width:32px;padding:5px 6px;text-align:center;">\n        </div>\n      </div>\n      <hr style="margin:16px 0;">\n      <div style="margin-bottom:16px;">\n        <label>在宅/外出実績: <select id="kh-autofill-homework" style="padding:4px 6px;margin-left:6px;"></select></label>\n        <div id="kh-autofill-partial-range" style="display:none;margin-top:10px;">\n          在宅勤務時間帯:\n          <input type="text" id="kh-autofill-partial-sth" style="width:32px;padding:5px 6px;text-align:center;">\n          :\n          <input type="text" id="kh-autofill-partial-stm" style="width:32px;padding:5px 6px;text-align:center;">\n          ～\n          <input type="text" id="kh-autofill-partial-eth" style="width:32px;padding:5px 6px;text-align:center;">\n          :\n          <input type="text" id="kh-autofill-partial-etm" style="width:32px;padding:5px 6px;text-align:center;">\n          <span id="kh-autofill-partial-auto-note" style="display:none;color:#888;font-size:13px;">(空欄で自動配分)</span>\n        </div>\n      </div>\n      <hr style="margin:16px 0;">\n      <div style="font-weight:bold;margin-bottom:10px;">登録番号（時間を空欄にすると、実働時間から他の登録番号を差し引いた残りを均等割りします）</div>\n      <div id="kh-autofill-reg-rows" style="margin-bottom:10px;"></div>\n      <button type="button" id="kh-autofill-add-row" style="font-size:15px;padding:6px 14px;">+ 登録番号を追加</button>\n      <div id="kh-autofill-footer" style="position:sticky;bottom:-24px;margin:16px -28px -24px;padding:14px 28px 24px;background:#fff;border-top:1px solid #ddd;">\n        <button type="button" id="kh-autofill-submit" style="background:#6633FF;color:#fff;border:none;border-radius:4px;padding:12px 26px;font-weight:bold;font-size:16px;cursor:pointer;margin-right:10px;">一括登録</button>\n        <button type="button" id="kh-autofill-cancel" style="background:#999;color:#fff;border:none;border-radius:4px;padding:12px 26px;font-weight:bold;font-size:16px;cursor:pointer;">キャンセル</button>\n      </div>\n    `;overlay.appendChild(panel);const selected=new Set(restoreDays||[]);const grid=panel.querySelector("#kh-autofill-cal-grid");const countEl=panel.querySelector("#kh-autofill-count");function renderGrid(){renderCalendarGrid(grid,year,month,d=>{const info=monthStatus[String(d.day)];const status=info?info.status:"blank";const isHoliday=d.isHoliday||info&&info.isHoliday;const isLeave=info&&info.isLeave;const cell=document.createElement("div");cell.className="kh-day-cell status-"+status+(isLeave?" kh-leave":"")+(isHoliday?" kh-holiday":"")+(selected.has(d.key)?" selected":"");cell.textContent=`${d.day}`;cell.addEventListener("click",()=>{if(selected.has(d.key))selected.delete(d.key);else selected.add(d.key);renderGrid()});return cell});countEl.textContent=`${selected.size}件選択中`}renderGrid();panel.querySelector("#kh-autofill-clear").addEventListener("click",()=>{selected.clear();renderGrid()});panel.querySelector("#kh-autofill-weekday-select").addEventListener("click",()=>{selected.clear();buildMonthDays(year,month).forEach(d=>{const info=monthStatus[String(d.day)];const isHoliday=d.isHoliday||info&&info.isHoliday;if(!isHoliday)selected.add(d.key)});renderGrid()});panel.querySelector("#kh-autofill-workday-select").addEventListener("click",()=>{selected.clear();buildMonthDays(year,month).forEach(d=>{const info=monthStatus[String(d.day)];const isHoliday=d.isHoliday||info&&info.isHoliday;const isLeave=info&&info.isLeave;if(!isHoliday&&!isLeave)selected.add(d.key)});renderGrid()});panel.querySelector("#kh-autofill-error-select").addEventListener("click",()=>{selected.clear();buildMonthDays(year,month).forEach(d=>{const info=monthStatus[String(d.day)];const status=info?info.status:"blank";if(status==="error")selected.add(d.key)});renderGrid()});panel.querySelector("#kh-autofill-help-btn").addEventListener("click",()=>{const helpPanel=panel.querySelector("#kh-autofill-help-panel");helpPanel.style.display=helpPanel.style.display==="none"?"block":"none"});const homeworkSelect=panel.querySelector("#kh-autofill-homework");const hasSavedHomeWorkType=!!savedPrefs&&"homeWorkType"in savedPrefs;const savedHomeWorkType=hasSavedHomeWorkType?savedPrefs.homeWorkType:undefined;HOME_WORK_TYPE_OPTIONS.forEach(([value,label])=>{const opt=document.createElement("option");opt.value=value;opt.textContent=label;if(hasSavedHomeWorkType?value===savedHomeWorkType:value==="3")opt.selected=true;homeworkSelect.appendChild(opt)});const partialRangeDiv=panel.querySelector("#kh-autofill-partial-range");const partialSthEl=panel.querySelector("#kh-autofill-partial-sth");const partialStmEl=panel.querySelector("#kh-autofill-partial-stm");const partialEthEl=panel.querySelector("#kh-autofill-partial-eth");const partialEtmEl=panel.querySelector("#kh-autofill-partial-etm");const partialAutoNoteEl=panel.querySelector("#kh-autofill-partial-auto-note");const savedPartialRange=savedPrefs&&savedPrefs.partialRange||{};partialSthEl.value=savedPartialRange.stH||"";partialStmEl.value=savedPartialRange.stM||"";partialEthEl.value=savedPartialRange.etH||"";partialEtmEl.value=savedPartialRange.etM||"";function updatePartialRangeVisibility(){const needsPartialRange=homeworkSelect.value==="2"||homeworkSelect.value==="4";partialRangeDiv.style.display=needsPartialRange?"block":"none";partialAutoNoteEl.style.display=homeworkSelect.value==="2"?"inline":"none"}homeworkSelect.addEventListener("change",updatePartialRangeVisibility);updatePartialRangeVisibility();const timeStdEl=panel.querySelector("#kh-autofill-time-std");const timeSthEl=panel.querySelector("#kh-autofill-time-sth");const timeStmEl=panel.querySelector("#kh-autofill-time-stm");const timeEtdEl=panel.querySelector("#kh-autofill-time-etd");const timeEthEl=panel.querySelector("#kh-autofill-time-eth");const timeEtmEl=panel.querySelector("#kh-autofill-time-etm");const savedManualTimeRange=savedPrefs&&savedPrefs.manualTimeRange||{};timeStdEl.value=savedManualTimeRange.stD||"0";timeSthEl.value=savedManualTimeRange.stH||"";timeStmEl.value=savedManualTimeRange.stM||"";timeEtdEl.value=savedManualTimeRange.etD||"0";timeEthEl.value=savedManualTimeRange.etH||"";timeEtmEl.value=savedManualTimeRange.etM||"";const regRowsEl=panel.querySelector("#kh-autofill-reg-rows");let regRows=savedPrefs&&Array.isArray(savedPrefs.regRows)&&savedPrefs.regRows.length>0?savedPrefs.regRows.map(r=>({code:r.code||"",h:r.h||"",m:r.m||""})):[{code:"",h:"",m:""}];function renderRegRows(){regRowsEl.innerHTML="";regRows.forEach((r,i)=>{const row=document.createElement("div");row.style.cssText="display:flex;gap:10px;align-items:center;margin-bottom:10px;";row.innerHTML=`\n          <input type="text" data-i="${i}" class="kh-autofill-reg-code" placeholder="登録番号" value="${r.code}" style="width:160px;padding:5px 6px;">\n          <input type="text" data-i="${i}" class="kh-autofill-reg-h" value="${r.h}" style="width:32px;padding:5px 6px;text-align:center;">\n          :\n          <input type="text" data-i="${i}" class="kh-autofill-reg-m" value="${r.m}" style="width:32px;padding:5px 6px;text-align:center;">\n          <span style="color:#888;font-size:13px;">(空欄で自動配分)</span>\n          <button type="button" class="kh-autofill-reg-remove" data-i="${i}" style="font-size:14px;padding:5px 12px;" ${regRows.length<=1?"disabled":""}>削除</button>\n        `;regRowsEl.appendChild(row)});regRowsEl.querySelectorAll(".kh-autofill-reg-code").forEach(el=>el.addEventListener("input",e=>{regRows[Number(e.target.dataset.i)].code=e.target.value}));regRowsEl.querySelectorAll(".kh-autofill-reg-h").forEach(el=>el.addEventListener("input",e=>{regRows[Number(e.target.dataset.i)].h=e.target.value}));regRowsEl.querySelectorAll(".kh-autofill-reg-m").forEach(el=>el.addEventListener("input",e=>{regRows[Number(e.target.dataset.i)].m=e.target.value}));regRowsEl.querySelectorAll(".kh-autofill-reg-remove").forEach(el=>el.addEventListener("click",e=>{const i=Number(e.target.dataset.i);regRows.splice(i,1);renderRegRows()}))}renderRegRows();panel.querySelector("#kh-autofill-add-row").addEventListener("click",()=>{regRows.push({code:"",h:"",m:""});renderRegRows()});panel.querySelector("#kh-autofill-cancel").addEventListener("click",()=>overlay.remove());panel.querySelector("#kh-autofill-submit").addEventListener("click",async()=>{const dateKeys=Array.from(selected).sort((a,b)=>Number(a.split("_")[2])-Number(b.split("_")[2]));const registrations=regRows.filter(r=>r.code.trim()).map(r=>{const hTrim=r.h.trim();const mTrim=r.m.trim();const isBlank=hTrim===""&&mTrim==="";return{code:r.code.trim(),h:isBlank?null:Number(hTrim||0),m:isBlank?null:Number(mTrim||0)}});if(dateKeys.length===0){alert("日付を1件以上選択してください。");return}const partialRange={stH:partialSthEl.value.trim(),stM:partialStmEl.value.trim(),etH:partialEthEl.value.trim(),etM:partialEtmEl.value.trim()};if(homeworkSelect.value==="4"&&Object.values(partialRange).every(v=>v==="")){alert("在宅勤務時間帯を入力してください。");return}const manualTimeRange={stH:timeSthEl.value.trim(),stM:timeStmEl.value.trim(),etH:timeEthEl.value.trim(),etM:timeEtmEl.value.trim()};const dayRange={stD:timeStdEl.value,etD:timeEtdEl.value};if(Number(dayRange.etD)<Number(dayRange.stD)){alert("終了日が開始日より過去になっています。実行時間の日付選択(前日/当日/翌日)を確認してください。");return}const useManualTimeRange=Object.values(manualTimeRange).some(v=>v!=="")?manualTimeRange:null;await chrome.storage.local.set({[AUTOFILL_PREFS_KEY]:{homeWorkType:homeworkSelect.value,regRows:regRows,partialRange:partialRange,manualTimeRange:{...manualTimeRange,...dayRange}}});overlay.remove();await startAutoFillJob(dateKeys,registrations,homeworkSelect.value,partialRange,useManualTimeRange,dayRange,monthStatus)});return overlay}async function startAutoFillJob(dateKeys,registrations,homeWorkType,partialRange,manualTimeRange,dayRange,monthStatus){const times={};const commutes={};const pcDataMap={};dateKeys.forEach(key=>{const day=key.split("_")[2];const info=monthStatus[day];commutes[key]=info&&info.commuteData||null;pcDataMap[key]=info&&info.pcData||null;if(manualTimeRange){times[key]={stH:Number(manualTimeRange.stH)||0,stM:Number(manualTimeRange.stM)||0,etH:Number(manualTimeRange.etH)||0,etM:Number(manualTimeRange.etM)||0,stD:dayRange.stD,etD:dayRange.etD};return}const auto=info&&info.timeRange||{stH:9,stM:0,etH:17,etM:30};times[key]={...auto,stD:dayRange.stD,etD:dayRange.etD}});const job={active:true,kind:"autoFill",queue:dateKeys,currentIndex:0,config:{registrations:registrations,homeWorkType:homeWorkType,partialRange:partialRange},times:times,commutes:commutes,pcData:pcDataMap,detailStep:undefined,log:[],startedAt:Date.now()};await setJob(job);await appendLog(job,`=== 一括入力${job.queue.length}件を開始します ===`);await updateStatusPanel();await processIfJobActive();await updateStatusPanel()}async function injectAutoFillButtonIfNeeded(){if(document.getElementById("kh-autofill-btn"))return;const grid=document.getElementById("APPROVALGRD");if(!grid)return;const displayed=getDisplayedListYearMonth();if(!displayed)return;const btn=document.createElement("button");btn.id="kh-autofill-btn";btn.type="button";btn.textContent="一括入力";btn.style.cssText="background:#6633FF;color:#fff;border:none;border-radius:4px;padding:6px 16px;font-weight:bold;cursor:pointer;margin:8px 0;display:block;";btn.addEventListener("click",async()=>{const monthStatus=await getCachedMonthStatus(displayed.year,displayed.month);const savedPrefs=(await chrome.storage.local.get(AUTOFILL_PREFS_KEY))[AUTOFILL_PREFS_KEY]||null;const job=await getJob();const restoreDays=job&&job.failed&&job.kind==="autoFill"?job.queue:null;document.body.appendChild(buildAutoFillOverlay(displayed.year,displayed.month,monthStatus,savedPrefs,restoreDays))});grid.parentNode.insertBefore(btn,grid)}function setFieldByName(name,value){if(value===undefined||value===null)return false;const el=document.querySelector(`[name="${CSS.escape(name)}"]`);if(!el)return false;el.value=value;return true}function getFieldValue(name){const el=document.querySelector(`[name="${CSS.escape(name)}"]`);return el?el.value:undefined}async function ensureRowCount(tableId,namePrefix,colArg,desiredCount,delMessage){let current=document.querySelectorAll(`select[name^="${namePrefix}"]`).length;let guard=0;while(current<desiredCount&&guard<20){await callPageFn("addRow",[tableId,colArg]);await sleep(30);current=document.querySelectorAll(`select[name^="${namePrefix}"]`).length;guard++}guard=0;while(current>desiredCount&&current>1&&guard<20){await callPageFn("delRow",[tableId,delMessage]);await sleep(30);current=document.querySelectorAll(`select[name^="${namePrefix}"]`).length;guard++}}function captureCurrentFormData(){const fd={};fd.kinmuType=getFieldValue("KNMCDS");fd.workStart={day:getFieldValue("KNMTMRNGSTD"),h:getFieldValue("KNMTMRNGSTH"),m:getFieldValue("KNMTMRNGSTM")};fd.workEnd={day:getFieldValue("KNMTMRNGETD"),h:getFieldValue("KNMTMRNGETH"),m:getFieldValue("KNMTMRNGETM")};const diffInput=document.querySelector('input[name="RTTLVAL195_3_12"]');fd.diffReasonCode=diffInput?diffInput.value:"0";fd.homeWorkType=getFieldValue("GI_COMBOBOX13_Seq0S");fd.homeWorkStart={day:getFieldValue("GI_TIMERANGE14_Seq0STD"),h:getFieldValue("GI_TIMERANGE14_Seq0STH"),m:getFieldValue("GI_TIMERANGE14_Seq0STM")};fd.homeWorkEnd={day:getFieldValue("GI_TIMERANGE14_Seq0ETD"),h:getFieldValue("GI_TIMERANGE14_Seq0ETH"),m:getFieldValue("GI_TIMERANGE14_Seq0ETM")};fd.restCorrection=getFieldValue("SGYCD2S");fd.restCorrectionTime={h:getFieldValue("SGYMI2H"),m:getFieldValue("SGYMI2M")};fd.restCorrectionNight=getFieldValue("SGYCD3S");fd.restCorrectionNightTime={h:getFieldValue("SGYMI3H"),m:getFieldValue("SGYMI3M")};fd.privateCar=getFieldValue("GI_COMBOBOX3_Seq0S");fd.report=getFieldValue("JSKMM")||"";fd.flexRest=[];document.querySelectorAll('select[name^="KRYCD3_Seq"]').forEach(sel=>{const m=sel.name.match(/KRYCD3_Seq(\d+)S/);if(!m||sel.value==="DEFAULT_CODE")return;const n=m[1];fd.flexRest.push({code:sel.value,start:{day:getFieldValue(`HKTTMRNG3_Seq${n}STD`),h:getFieldValue(`HKTTMRNG3_Seq${n}STH`),m:getFieldValue(`HKTTMRNG3_Seq${n}STM`)},end:{day:getFieldValue(`HKTTMRNG3_Seq${n}ETD`),h:getFieldValue(`HKTTMRNG3_Seq${n}ETH`),m:getFieldValue(`HKTTMRNG3_Seq${n}ETM`)}})});fd.personalOuting=[];document.querySelectorAll('select[name^="KRYCD5_Seq"]').forEach(sel=>{const m=sel.name.match(/KRYCD5_Seq(\d+)S/);if(!m||sel.value==="DEFAULT_CODE")return;const n=m[1];fd.personalOuting.push({code:sel.value,start:{day:getFieldValue(`HKTTMRNG5_Seq${n}STD`),h:getFieldValue(`HKTTMRNG5_Seq${n}STH`),m:getFieldValue(`HKTTMRNG5_Seq${n}STM`)},end:{day:getFieldValue(`HKTTMRNG5_Seq${n}ETD`),h:getFieldValue(`HKTTMRNG5_Seq${n}ETH`),m:getFieldValue(`HKTTMRNG5_Seq${n}ETM`)}})});fd.projects=[];const container=document.getElementById("PM_PANEL_ENTRY_TIME_WIDGET_CONTAINER_AREA");if(container){const titles=container.querySelectorAll(".PmPanelEntryTimeWidgetTitleStyle");const idInputs=container.querySelectorAll(".TIME_WIDGET_ID_KEY");titles.forEach((titleEl,i)=>{const m=titleEl.textContent.match(/【([^】]+)】/);if(!m)return;const idInput=idInputs[i];if(!idInput)return;const widgetId=idInput.value;const hInput=document.getElementById(`PmDdEntryTimeInputWidget_${widgetId}H`);const mInput=document.getElementById(`PmDdEntryTimeInputWidget_${widgetId}M`);fd.projects.push({code:m[1],h:hInput?hInput.value||"0":"0",m:mInput?mInput.value||"0":"0"})})}return fd}async function trySearchAndInsertOnce(entry,log){const codeInput=document.querySelector('[name="COLLECT_ID_PRJ_CD_WIDGET_KEY"]');if(!codeInput)throw new Error("工数検索欄が見つかりません");codeInput.value=entry.code;log(`「${entry.code}」を検索中...`);await callPageFn("searchProject");const searchResult=await waitFor(()=>{const list=document.getElementById("AJAX_BASE_FOR_SEARCHED_LIST");if(!list)return null;const rows=list.querySelectorAll("tr.pm_common_tr");const matches=[];for(const r of rows){const tds=r.querySelectorAll("td");if(tds.length>=2&&tds[1].textContent.trim().includes(entry.code))matches.push(r)}if(matches.length===1)return{row:matches[0]};if(matches.length>1)return{multiple:true,count:matches.length};return null},8e3,50);if(!searchResult){log(`✗「${entry.code}」検索結果が見つかりませんでした。`);return null}if(searchResult.multiple){log(`✗「${entry.code}」に部分一致する登録番号が${searchResult.count}件見つかりました。`);const err=new Error(`「${entry.code}」に部分一致する登録番号が複数見つかったため、この登録番号をスキップします`);err.multipleMatch=true;err.code=entry.code;throw err}const row=searchResult.row;const checkbox=row.querySelector('input[type="checkbox"]');if(!checkbox){log(`✗「${entry.code}」のチェックボックスが見つかりません。`);return null}if(!checkbox.checked)checkbox.click();await sleep(200);const container=document.getElementById("PM_PANEL_ENTRY_TIME_WIDGET_CONTAINER_AREA");const prevCount=container?container.querySelectorAll(".PmPanelEntryTimeWidgetTitleStyle").length:0;log(`「${entry.code}」を編集欄に追加中...`);await callPageFn("insertFromSearch");const newTitleEl=await waitFor(()=>{const c=document.getElementById("PM_PANEL_ENTRY_TIME_WIDGET_CONTAINER_AREA");if(!c)return null;const titles=c.querySelectorAll(".PmPanelEntryTimeWidgetTitleStyle");return titles.length>prevCount?titles[titles.length-1]:null},8e3,50);return newTitleEl||null}async function clearExistingProjectEntries(log){const container=document.getElementById("PM_PANEL_ENTRY_TIME_WIDGET_CONTAINER_AREA");if(!container)return;let removedCount=0;let guard=0;while(guard<20){const idInputs=container.querySelectorAll(".TIME_WIDGET_ID_KEY");if(idInputs.length===0)break;const beforeCount=idInputs.length;await callPageFn("removeTimeWidget",[idInputs[0].value]);await waitFor(()=>{const c=document.getElementById("PM_PANEL_ENTRY_TIME_WIDGET_CONTAINER_AREA");const inputs=c?c.querySelectorAll(".TIME_WIDGET_ID_KEY"):[];return inputs.length<beforeCount?true:null},5e3,50);removedCount++;guard++}if(removedCount>0)log(`既存の工数登録 ${removedCount}件を削除しました。`)}async function addOneProjectEntry(entry,log){let newTitleEl=await trySearchAndInsertOnce(entry,log);if(!newTitleEl){log(`「${entry.code}」の追加に失敗したため、もう一度試します...`);await sleep(150);newTitleEl=await trySearchAndInsertOnce(entry,log)}if(!newTitleEl){log(`✗「${entry.code}」の編集欄への追加に失敗しました。`);const codeInput=document.querySelector('[name="COLLECT_ID_PRJ_CD_WIDGET_KEY"]');if(codeInput)codeInput.value="";return false}const container=document.getElementById("PM_PANEL_ENTRY_TIME_WIDGET_CONTAINER_AREA");const idInputs=container?container.querySelectorAll(".TIME_WIDGET_ID_KEY"):[];const idInput=idInputs[idInputs.length-1];if(!idInput){log(`✗「${entry.code}」の入力欄IDが取得できませんでした。`);return false}const widgetId=idInput.value;const hInput=document.getElementById(`PmDdEntryTimeInputWidget_${widgetId}H`);const mInput=document.getElementById(`PmDdEntryTimeInputWidget_${widgetId}M`);if(!hInput||!mInput){log(`✗「${entry.code}」の時間入力欄が見つかりませんでした。`);return false}hInput.value=String(entry.h);mInput.value=String(entry.m);hInput.dispatchEvent(new Event("change",{bubbles:true}));mInput.dispatchEvent(new Event("change",{bubbles:true}));log(`✓「${entry.code}」 ${entry.h}時間${entry.m}分 を入力しました。`);return true}async function fillDetailPage(fd,log,sections){sections=sections||{};const enabled=key=>sections[key]!==false;if(enabled("knm")){setFieldByName("KNMCDS",fd.kinmuType);setFieldByName("KNMTMRNGSTD",fd.workStart.day);setFieldByName("KNMTMRNGSTH",fd.workStart.h);setFieldByName("KNMTMRNGSTM",fd.workStart.m);setFieldByName("KNMTMRNGETD",fd.workEnd.day);setFieldByName("KNMTMRNGETH",fd.workEnd.h);setFieldByName("KNMTMRNGETM",fd.workEnd.m);const diffInput=document.querySelector('input[name="RTTLVAL195_3_12"]');if(diffInput&&fd.diffReasonCode)diffInput.value=fd.diffReasonCode}if(enabled("home")){setFieldByName("GI_COMBOBOX13_Seq0S",fd.homeWorkType);setFieldByName("GI_TIMERANGE14_Seq0STD",fd.homeWorkStart.day);setFieldByName("GI_TIMERANGE14_Seq0STH",fd.homeWorkStart.h);setFieldByName("GI_TIMERANGE14_Seq0STM",fd.homeWorkStart.m);setFieldByName("GI_TIMERANGE14_Seq0ETD",fd.homeWorkEnd.day);setFieldByName("GI_TIMERANGE14_Seq0ETH",fd.homeWorkEnd.h);setFieldByName("GI_TIMERANGE14_Seq0ETM",fd.homeWorkEnd.m)}if(enabled("restCorrection")){setFieldByName("SGYCD2S",fd.restCorrection);setFieldByName("SGYMI2H",fd.restCorrectionTime&&fd.restCorrectionTime.h);setFieldByName("SGYMI2M",fd.restCorrectionTime&&fd.restCorrectionTime.m);setFieldByName("SGYCD3S",fd.restCorrectionNight);setFieldByName("SGYMI3H",fd.restCorrectionNightTime&&fd.restCorrectionNightTime.h);setFieldByName("SGYMI3M",fd.restCorrectionNightTime&&fd.restCorrectionNightTime.m)}if(enabled("privateCar")){setFieldByName("GI_COMBOBOX3_Seq0S",fd.privateCar)}if(enabled("report")){setFieldByName("JSKMM",fd.report||"")}if(enabled("flexRest")){const flexEntries=fd.flexRest||[];await ensureRowCount("HKTNO","KRYCD3_Seq",3,Math.max(1,flexEntries.length),"行数を０行にすることはできません。");document.querySelectorAll('select[name^="KRYCD3_Seq"]').forEach((sel,i)=>{const m=sel.name.match(/KRYCD3_Seq(\d+)S/);const n=m?m[1]:i;const entry=flexEntries[i];sel.value=entry?entry.code:"DEFAULT_CODE";if(entry){setFieldByName(`HKTTMRNG3_Seq${n}STD`,entry.start.day);setFieldByName(`HKTTMRNG3_Seq${n}STH`,entry.start.h);setFieldByName(`HKTTMRNG3_Seq${n}STM`,entry.start.m);setFieldByName(`HKTTMRNG3_Seq${n}ETD`,entry.end.day);setFieldByName(`HKTTMRNG3_Seq${n}ETH`,entry.end.h);setFieldByName(`HKTTMRNG3_Seq${n}ETM`,entry.end.m)}})}if(enabled("outing")){const outingEntries=fd.personalOuting||[];await ensureRowCount("HKTG","KRYCD5_Seq",3,Math.max(1,outingEntries.length),"行数を０行にすることはできません。");document.querySelectorAll('select[name^="KRYCD5_Seq"]').forEach((sel,i)=>{const m=sel.name.match(/KRYCD5_Seq(\d+)S/);const n=m?m[1]:i;const entry=outingEntries[i];sel.value=entry?entry.code:"DEFAULT_CODE";if(entry){setFieldByName(`HKTTMRNG5_Seq${n}STD`,entry.start.day);setFieldByName(`HKTTMRNG5_Seq${n}STH`,entry.start.h);setFieldByName(`HKTTMRNG5_Seq${n}STM`,entry.start.m);setFieldByName(`HKTTMRNG5_Seq${n}ETD`,entry.end.day);setFieldByName(`HKTTMRNG5_Seq${n}ETH`,entry.end.h);setFieldByName(`HKTTMRNG5_Seq${n}ETM`,entry.end.m)}})}if(!enabled("projects"))return;await clearExistingProjectEntries(log);const projects=(fd.projects||[]).filter(p=>p.code);for(const entry of projects){try{await addOneProjectEntry(entry,log)}catch(e){log(`✗ エラー: ${e.message}`)}await sleep(40)}}function readDetailPageErrorText(){const el=document.querySelector(".error")||document.querySelector('[id^="DYERR_error"]');return el?el.textContent.replace(/\s+/g," ").trim():null}function formatSkippedEntries(skipped){const groups=new Map();const lines=[];skipped.forEach(s=>{if(s.reasonHasDate){lines.push({order:lines.length+groups.size,text:s.reason});return}if(!groups.has(s.reason))groups.set(s.reason,{order:lines.length+groups.size,dateKeys:[]});groups.get(s.reason).dateKeys.push(s.dateKey)});const grouped=Array.from(groups.entries()).map(([reason,g])=>({order:g.order,text:`${g.dateKeys.join(",")}: ${reason}`}));return lines.concat(grouped).sort((a,b)=>a.order-b.order).map(x=>x.text)}async function backToListSkippingDay(job,dateKey,log,reason){const pageError=readDetailPageErrorText();const fullReason=pageError?`${reason}(画面のエラー: ${pageError})`:reason;log(`✗ ${fullReason} この日をスキップします。`);job.skipped=job.skipped||[];job.skipped.push({dateKey:dateKey,reason:pageError||reason,reasonHasDate:!!pageError});job.detailStep=undefined;job.currentIndex+=1;await setJob(job);await sleep(30);const backImg=document.querySelector('img[name^="btnGoBack"]');if(backImg){backImg.click()}else{log("✗ 戻るボタンが見つかりません。処理を中断します。");job.active=false;job.failed=true;await setJob(job)}}function computeAutoHomeWorkRange(job,dateKey,log){const exec=job.times[dateKey];const commute=job.commutes&&job.commutes[dateKey];if(!exec||!commute){log(`△ ${dateKey}: 入退館客観データが取得できないため、在宅勤務なしとして登録します。`);return null}const execMin=rangeToMinutes(exec);const execStart=execMin.start;const execEnd=execMin.end;const commuteMin=rangeToMinutes(commute);const commuteStart=commuteMin.start;const commuteEnd=commuteMin.end;const morningGap=Math.max(0,commuteStart-execStart);const eveningGap=Math.max(0,execEnd-commuteEnd);if(morningGap===0&&eveningGap===0){log(`${dateKey}: 実行時間が入退館客観データの範囲に収まっているため、在宅勤務なしとして登録します。`);return null}if(morningGap>0&&eveningGap>0){log(`△ ${dateKey}: 出社前(${morningGap}分)・退社後(${eveningGap}分)の両方に在宅勤務の可能性がある時間がありますが、大きい方のみ自動算出します。必要に応じて手動で確認してください。`)}const useMorning=morningGap>=eveningGap;const stMin=useMorning?execStart:commuteEnd;const etMin=useMorning?commuteStart:execEnd;const st=floorTo10(Math.floor(stMin/60),stMin%60);const et=floorTo10(Math.floor(etMin/60),etMin%60);log(`${dateKey}: 在宅勤務時間帯を ${st.h}:${String(st.m).padStart(2,"0")}〜${et.h}:${String(et.m).padStart(2,"0")} として自動算出しました。`);return{stH:st.h,stM:st.m,etH:et.h,etM:et.m}}function determineAutoHomeWorkType(job,dateKey,log){const pcData=job.pcData&&job.pcData[dateKey];const commute=job.commutes&&job.commutes[dateKey];if(!pcData&&!commute){log(`${dateKey}: 自動判別 - PC客観データ・入退館客観データともになしのため「在宅勤務なし」とします。`);return"1"}if(pcData&&!commute){log(`${dateKey}: 自動判別 - PC客観データのみあるため「終日在宅（通勤出社なし）」とします。`);return"3"}if(!pcData&&commute){log(`△ ${dateKey}: 自動判別 - 入退館客観データのみでPC客観データがないため「在宅勤務なし」とします。`);return"1"}const stDiff=Math.abs(rangeToMinutes(pcData).start-rangeToMinutes(commute).start);const etDiff=Math.abs(rangeToMinutes(pcData).end-rangeToMinutes(commute).end);const maxDiff=Math.max(stDiff,etDiff);if(maxDiff>=30){log(`${dateKey}: 自動判別 - PC客観データと入退館客観データの差が${maxDiff}分あるため「一部在宅（通勤出社あり）」とします。`);return"2"}log(`${dateKey}: 自動判別 - PC客観データと入退館客観データの差が${maxDiff}分(30分未満)のため「在宅勤務なし」とします。`);return"1"}function applyHomeWorkFields(job,dateKey,log){if(!job.config.homeWorkType)return;let type=job.config.homeWorkType;if(type==="auto"){type=determineAutoHomeWorkType(job,dateKey,log)}let range;if(type==="2"||type==="4"){const p=job.config.partialRange||{};const hasManualRange=Object.values(p).some(v=>v!==""&&v!==null&&v!==undefined);if(!hasManualRange&&type==="2"){const auto=computeAutoHomeWorkRange(job,dateKey,log);if(auto){range=auto}else{type="1";range={stH:0,stM:0,etH:0,etM:0}}}else{range={stH:Number(p.stH)||0,stM:Number(p.stM)||0,etH:Number(p.etH)||0,etM:Number(p.etM)||0}}}else{range={stH:0,stM:0,etH:0,etM:0}}setFieldByName("GI_COMBOBOX13_Seq0S",type);setFieldByName("GI_TIMERANGE14_Seq0STD","0");setFieldByName("GI_TIMERANGE14_Seq0STH",range.stH);setFieldByName("GI_TIMERANGE14_Seq0STM",range.stM);setFieldByName("GI_TIMERANGE14_Seq0ETD","0");setFieldByName("GI_TIMERANGE14_Seq0ETH",range.etH);setFieldByName("GI_TIMERANGE14_Seq0ETM",range.etM)}async function handleAutoFillDetailStep(job,dateKey,log){if(job.detailStep==="submitted"){await backToListSkippingDay(job,dateKey,log,`「次へ」を押しても確認画面へ進めませんでした(工数や在宅/外出実績の入力内容に問題がある可能性があります)。`);return}if(job.detailStep==="awaitingCalc"){await waitFor(()=>document.querySelector('[name="COLLECT_ID_PRJ_CD_WIDGET_KEY"]'),5e3,80);await sleep(30);const jH=document.getElementById("JTDMIH");const jM=document.getElementById("JTDMIM");const workedMinutes=jH&&jM?Number(jH.textContent)*60+Number(jM.textContent):null;if(workedMinutes===null){await backToListSkippingDay(job,dateKey,log,`実働時間が読み取れませんでした。`);return}const registrations=job.config.registrations;const specified=registrations.filter(r=>r.h!==null&&r.h!==undefined);const blanks=registrations.filter(r=>r.h===null||r.h===undefined);const toMinutes=r=>Math.round(Number(r.h)*60+Number(r.m||0));const specifiedMinutes=specified.reduce((sum,r)=>sum+toMinutes(r),0);const remaining=workedMinutes-specifiedMinutes;const entries=specified.map(r=>{const total=toMinutes(r);return{code:r.code,h:Math.floor(total/60),m:total%60}});if(blanks.length>0){const totalToSplit=remaining>0?Math.floor(remaining/10)*10:0;const share=Math.floor(totalToSplit/10/blanks.length)*10;blanks.forEach((r,i)=>{const isLast=i===blanks.length-1;const minutes=isLast?totalToSplit-share*(blanks.length-1):share;entries.push({code:r.code,h:Math.floor(minutes/60),m:minutes%60})})}if(remaining!==0){log(`△ 指定済み工数の合計(${specifiedMinutes}分)が実働時間(${workedMinutes}分)と一致していません。登録は続行します。`)}const ambiguousCodes=[];for(const entry of entries){try{await addOneProjectEntry(entry,log)}catch(e){if(e.multipleMatch){ambiguousCodes.push(e.code);continue}await backToListSkippingDay(job,dateKey,log,e.message);return}await sleep(60)}if(ambiguousCodes.length>0){const reason=`${ambiguousCodes.map(c=>`「${c}」`).join("")}に部分一致する登録番号が複数見つかったため、この登録番号をスキップします`;await backToListSkippingDay(job,dateKey,log,reason);return}applyHomeWorkFields(job,dateKey,log);job.detailStep="submitted";await appendLog(job,"「次へ」を押して確認画面へ進みます。");await updateStatusPanel();await sleep(400);neutralizePasswordFields();const nextBtn=document.querySelector('[id^="btnNext"]');if(nextBtn){nextBtn.click()}else{await appendLog(job,"✗「次へ」ボタンが見つかりません。処理を中断します。");job.active=false;job.failed=true;await setJob(job)}return}const times=job.times[dateKey];if(times){setFieldByName("KNMTMRNGSTD",times.stD!==undefined?times.stD:"0");setFieldByName("KNMTMRNGSTH",times.stH);setFieldByName("KNMTMRNGSTM",times.stM);setFieldByName("KNMTMRNGETD",times.etD!==undefined?times.etD:"0");setFieldByName("KNMTMRNGETH",times.etH);setFieldByName("KNMTMRNGETM",times.etM)}await waitFor(()=>document.querySelector('[name="COLLECT_ID_PRJ_CD_WIDGET_KEY"]'),5e3,80);await sleep(30);await clearExistingProjectEntries(log);await appendLog(job,`${dateKey}: 実行時間を設定し、計算します。`);job.detailStep="awaitingCalc";await setJob(job);await sleep(100);neutralizePasswordFields();const calcBtn=document.querySelector('[id^="btnCalc"]');if(calcBtn){calcBtn.click()}else{await appendLog(job,"✗ 計算ボタンが見つかりません。処理を中断します。");job.active=false;job.failed=true;await setJob(job)}}let processing=false;async function processIfJobActive(){if(processing)return;const job=await getJob();if(!job||!job.active)return;const lastActivity=job.lastActivityAt||job.startedAt||0;if(lastActivity&&Date.now()-lastActivity>90*1e3){job.active=false;job.failed=true;await appendLog(job,"✗ 処理が長時間進行しなかったため自動中断しました。");return}if(isReauthVisible()){job.active=false;job.failed=true;await appendLog(job,"✗ 再認証が要求されたため処理を中断しました。ログイン状態を確認してください。");return}processing=true;try{const pageType=await waitFor(()=>{const t=detectPageType();if(t!=="other")return t;return findMenuRecoveryLink()?"other":null},1e4,150)||"other";const log=msg=>appendLog(job,msg);if(pageType!=="other"&&job.recoveryAttempts){job.recoveryAttempts=0;await setJob(job)}if(pageType==="list"){if(job.currentIndex>=job.queue.length){job.active=false;const doneMsg=job.kind==="leaveApply"?`=== 完了: 休暇${job.queue.length}件の申請が終わりました ===`:job.kind==="workdayApply"?`=== 完了: 出勤日${job.queue.length}件の申請が終わりました ===`:job.kind==="autoFill"?`=== 完了: 一括入力${job.queue.length}件の申請が終わりました ===`:`=== 完了: 他日${job.queue.length}件への入力が終わりました ===`;await appendLog(job,doneMsg);if(job.skipped&&job.skipped.length>0){await appendLog(job,`△ 登録できずスキップした日(${job.skipped.length}件):`);for(const line of formatSkippedEntries(job.skipped)){await appendLog(job,`　${line}`)}}return}const dateKey=job.queue[job.currentIndex];await appendLog(job,`--- [${job.currentIndex+1}/${job.queue.length}] ${dateKey} の詳細画面を開きます ---`);await updateStatusPanel();await sleep(30);neutralizePasswordFields();const clicked=clickDetailButton(dateKey);if(!clicked){await appendLog(job,`✗ ${dateKey} の詳細ボタンが見つかりません。この日をスキップします。`);job.skipped=job.skipped||[];job.skipped.push({dateKey:dateKey,reason:"詳細ボタンが見つかりませんでした"});job.currentIndex+=1;await setJob(job)}}else if(pageType==="detail"){const dateKey=getDetailDateKey();const expected=job.queue[job.currentIndex];if(dateKey!==expected){await appendLog(job,`✗ 想定と異なる日付（${dateKey}）が開かれました。安全のため処理を中断します。`);job.active=false;job.failed=true;return}await updateStatusPanel();if(job.kind==="autoFill"){try{await handleAutoFillDetailStep(job,dateKey,log)}catch(e){await appendLog(job,`✗ 一括入力の処理中に予期しないエラーが発生しました: ${e.message}。処理を中断します。`);job.active=false;job.failed=true;await setJob(job)}}else{if(job.detailStep==="submitted"){await backToListSkippingDay(job,dateKey,log,`「次へ」を押しても確認画面へ進めませんでした。`);return}if(job.kind==="leaveApply"||job.kind==="workdayApply"){await appendLog(job,`${dateKey} は勤務区分が自動選択済みのため入力をスキップします。`)}else{await waitFor(()=>document.querySelector('[name="COLLECT_ID_PRJ_CD_WIDGET_KEY"]'),5e3,80);await sleep(30);if(job.detailStep==="awaitingRestCorrectionCalc"){job.detailStep=undefined}else if((job.sections||{}).restCorrection!==false){const needsNormalReload=job.formData.restCorrection==="XX2010"&&!document.querySelector('[name="SGYMI2H"]');const needsNightReload=job.formData.restCorrectionNight==="XX3010"&&!document.querySelector('[name="SGYMI3H"]');if(needsNormalReload||needsNightReload){setFieldByName("SGYCD2S",job.formData.restCorrection);setFieldByName("SGYCD3S",job.formData.restCorrectionNight);await appendLog(job,`${dateKey}: 休憩補正の時間欄を表示するため再読み込みします。`);job.detailStep="awaitingRestCorrectionCalc";await setJob(job);neutralizePasswordFields();const triggerEl=document.querySelector(`[name="${needsNormalReload?"SGYCD2S":"SGYCD3S"}"]`);if(triggerEl)triggerEl.dispatchEvent(new Event("change",{bubbles:true}));return}}const projCount=(job.formData.projects||[]).filter(p=>p.code).length;await appendLog(job,`入力予定: 種別=${job.formData.kinmuType} 工数${projCount}件`);try{await fillDetailPage(job.formData,log,job.sections)}catch(e){await appendLog(job,`✗ 入力中にエラーが発生しました: ${e.message}（このまま次へ進みます）`)}}await appendLog(job,"「次へ」を押して確認画面へ進みます。");await updateStatusPanel();await sleep(200);neutralizePasswordFields();const nextBtn=document.querySelector('[id^="btnNext"]');if(nextBtn){job.detailStep="submitted";await setJob(job);nextBtn.click()}else{await appendLog(job,"✗「次へ」ボタンが見つかりません。処理を中断します。");job.active=false;job.failed=true;await setJob(job)}}}else if(pageType==="confirm"){await updateStatusPanel();const btn=document.querySelector('[id^="dSubmission"]');if(!btn){await appendLog(job,"✗ 確認画面の登録ボタンが見つかりません。処理を中断します。");job.active=false;job.failed=true;await setJob(job);return}const moveSelect=document.querySelector('select[name="move_emp_date"]');if(moveSelect)moveSelect.value="0";job.currentIndex+=1;job.detailStep=undefined;await appendLog(job,"「入力完了」を実行します。");await setJob(job);await sleep(30);neutralizePasswordFields();btn.click()}else{const recoveryLink=findMenuRecoveryLink();job.recoveryAttempts=(job.recoveryAttempts||0)+1;if(recoveryLink&&job.recoveryAttempts<=5){await appendLog(job,`△ 想定外の画面(メインメニュー等)に遷移したため、月一覧への復帰を試みます(${job.recoveryAttempts}/5)。`);await setJob(job);await sleep(150);recoveryLink.click()}else{job.active=false;job.failed=true;await appendLog(job,"✗ 想定外の画面に遷移したため処理を中断しました。ログイン状態を確認し、必要であれば選択日数を減らして再度お試しください。");await setJob(job)}}}finally{processing=false}}async function promotePendingIfReturnedToList(){const data=await chrome.storage.local.get(PENDING_KEY);const pending=data[PENDING_KEY];if(!pending||!pending.queue||pending.queue.length===0)return;if(!pending.confirmed){await chrome.storage.local.remove(PENDING_KEY);return}const job={active:true,kind:"bulkCopy",originDateKey:pending.originDateKey,formData:pending.formData,sections:pending.sections,queue:pending.queue,currentIndex:0,log:[],startedAt:Date.now()};await setJob(job);await appendLog(job,`=== ${pending.originDateKey} から一覧画面に戻りました。他日${job.queue.length}件への一括入力を開始します ===`);await chrome.storage.local.remove(PENDING_KEY)}function escapeHtml(str){return String(str).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}function copyTextToClipboard(text){const ta=document.createElement("textarea");ta.value=text;ta.style.position="fixed";ta.style.left="-9999px";document.body.appendChild(ta);ta.focus();ta.select();let ok=false;try{ok=document.execCommand("copy")}catch(e){ok=false}document.body.removeChild(ta);return ok}function saveTextAsFile(text,filename){const blob=new Blob([text],{type:"text/plain;charset=utf-8"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=filename;document.body.appendChild(a);a.click();document.body.removeChild(a);URL.revokeObjectURL(url)}async function updateStatusPanel(){const job=await getJob();let overlay=document.getElementById("kh-status-overlay");if(job&&job.active){if(!overlay){overlay=document.createElement("div");overlay.id="kh-status-overlay";overlay.style.cssText="position:fixed;inset:0;z-index:2147483647;background:rgba(0,0,0,0.85);display:flex;align-items:center;justify-content:center;";const panel=document.createElement("div");panel.id="kh-status-panel";panel.style.cssText="background:#fff;border:4px solid #6633FF;border-radius:10px;padding:20px 28px;font-size:18px;max-width:480px;min-width:320px;box-shadow:0 6px 24px rgba(0,0,0,0.4);color:#222;text-align:center;";overlay.appendChild(panel);document.body.appendChild(overlay)}const panel=document.getElementById("kh-status-panel");const lastLines=(job.log||[]).slice(-3).map(l=>`・${l.message}`).join("<br>");const progressLabel=job.kind==="leaveApply"?"⏳ 休暇を申請中":job.kind==="workdayApply"?"⏳ 出勤日を申請中":job.kind==="autoFill"?"⏳ 一括入力・申請中":"⏳ 他日へ反映中";panel.innerHTML=`\n        <div style="font-size:22px;font-weight:bold;color:#6633FF;margin-bottom:10px;">${progressLabel}</div>\n        <div style="font-weight:bold;margin-bottom:8px;">（${Math.min(job.currentIndex+1,job.queue.length)}/${job.queue.length}件）</div>\n        <div style="font-size:14px;color:#555;text-align:left;margin-bottom:14px;">${lastLines}</div>\n        <button id="kh-cancel-btn" style="background:#c0392b;color:#fff;border:none;border-radius:4px;padding:10px 20px;font-weight:bold;font-size:16px;cursor:pointer;">処理を中断する</button>\n      `;document.getElementById("kh-cancel-btn").addEventListener("click",async()=>{const j=await getJob();if(j){j.active=false;await appendLog(j,"✗ ユーザーにより処理が中断されました。")}overlay.remove()});overlay.dataset.wasActive="1";overlay.dataset.kind=job.kind||"bulkCopy"}else if(overlay&&overlay.dataset.wasActive==="1"){if(job&&job.failed){const fullLogText=(job.log||[]).map(l=>`${l.time} ${l.message}`).join("\n");overlay.innerHTML=`\n          <div style="background:#fff;border:4px solid #c0392b;border-radius:10px;padding:20px 28px;max-width:560px;width:92%;max-height:80vh;overflow-y:auto;box-shadow:0 6px 24px rgba(0,0,0,0.4);text-align:left;">\n            <div style="font-size:22px;font-weight:bold;color:#c0392b;margin-bottom:12px;">✗ エラーが発生しました</div>\n            <div style="font-size:15px;color:#333;margin-bottom:14px;">入力内容は保持されていますので、お手数ですが再度実行してください。何度か試してもうまくいかない場合は、下記のログを添えて管理者へご連絡ください。</div>\n            <div style="font-size:14px;color:#555;margin-bottom:14px;white-space:pre-wrap;word-break:break-all;">${escapeHtml(fullLogText)}</div>\n            <button id="kh-error-copy-btn" style="background:#6633FF;color:#fff;border:none;border-radius:4px;padding:10px 20px;font-weight:bold;font-size:16px;cursor:pointer;margin-right:8px;">ログをコピー</button>\n            <button id="kh-error-save-btn" style="background:#6633FF;color:#fff;border:none;border-radius:4px;padding:10px 20px;font-weight:bold;font-size:16px;cursor:pointer;margin-right:8px;">テキストで保存</button>\n            <button id="kh-error-close-btn" style="background:#999;color:#fff;border:none;border-radius:4px;padding:10px 20px;font-weight:bold;font-size:16px;cursor:pointer;">閉じる</button>\n          </div>\n        `;const copyBtn=document.getElementById("kh-error-copy-btn");copyBtn.addEventListener("click",()=>{const ok=copyTextToClipboard(fullLogText);copyBtn.textContent=ok?"コピーしました":"コピーに失敗しました";setTimeout(()=>{copyBtn.textContent="ログをコピー"},2e3)});document.getElementById("kh-error-save-btn").addEventListener("click",()=>{const ts=(new Date).toISOString().replace(/[:.]/g,"-");saveTextAsFile(fullLogText,`kintai-helper-log_${ts}.txt`)});document.getElementById("kh-error-close-btn").addEventListener("click",()=>overlay.remove());return}const doneLabel=overlay.dataset.kind==="leaveApply"?"休暇の申請が完了しました":overlay.dataset.kind==="workdayApply"?"出勤日の申請が完了しました":overlay.dataset.kind==="autoFill"?"一括入力・申請が完了しました":"他日への反映が完了しました";if(job&&job.skipped&&job.skipped.length>0){const skippedListHtml=formatSkippedEntries(job.skipped).map(line=>`<li>${escapeHtml(line)}</li>`).join("");overlay.innerHTML=`\n          <div style="background:#fff;border:4px solid #e67e22;border-radius:10px;padding:20px 28px;max-width:520px;width:92%;max-height:80vh;overflow-y:auto;box-shadow:0 6px 24px rgba(0,0,0,0.4);text-align:left;">\n            <div style="font-size:22px;font-weight:bold;color:#4caf50;margin-bottom:10px;">✓ ${doneLabel}</div>\n            <div style="font-size:16px;font-weight:bold;color:#e67e22;margin-bottom:8px;">△ 登録できずスキップした日が${job.skipped.length}件あります</div>\n            <ul style="margin:0 0 16px;padding-left:1.2em;font-size:14px;color:#333;">${skippedListHtml}</ul>\n            <button id="kh-skipped-close-btn" style="background:#6633FF;color:#fff;border:none;border-radius:4px;padding:10px 20px;font-weight:bold;font-size:16px;cursor:pointer;">閉じる</button>\n          </div>\n        `;document.getElementById("kh-skipped-close-btn").addEventListener("click",()=>overlay.remove());return}overlay.innerHTML=`<div style="background:#fff;border:4px solid #4caf50;border-radius:10px;padding:20px 28px;font-size:22px;font-weight:bold;color:#4caf50;box-shadow:0 6px 24px rgba(0,0,0,0.4);">✓ ${doneLabel}</div>`;setTimeout(()=>overlay.remove(),4e3)}}function ensureBulkStylesInjected(){if(document.getElementById("kh-bulk-style"))return;const style=document.createElement("style");style.id="kh-bulk-style";style.textContent=`\n      #kh-bulk-box { border: 2px solid #6633FF; border-radius: 6px; padding: 10px; margin: 8px 0; background: #f8f6ff; font-size: 15px; max-width: 1400px; width: 100%; }\n      #kh-bulk-box button { font-size: 15px; padding: 6px 14px; cursor: pointer; }\n      #kh-bulk-box .kh-checkbox-label { font-weight: bold; cursor: pointer; }\n      #kh-bulk-box .kh-cal-note { color: #555; margin: 6px 0; font-size: 13px; }\n      .kh-cal-legend { font-size: 13px; color: #555; margin-bottom: 6px; }\n      .kh-legend-dot { display: inline-block; width: 12px; height: 12px; border-radius: 2px; margin: 0 3px 0 8px; vertical-align: middle; border: 1px solid #999; }\n      .kh-cal-grid { display: grid; grid-template-columns: repeat(7, 1fr); gap: 5px; margin-bottom: 6px; }\n      .kh-cal-weekday-head { text-align: center; font-weight: bold; font-size: 13px; color: #555; padding: 2px; }\n      .kh-cal-weekday-head.kh-holiday { color: #ff0000; }\n      .kh-day-cell { min-height: 42px; border: 1px solid #ccc; border-radius: 4px; background: #fff; cursor: pointer; font-size: 14px; text-align: center; padding: 2px; line-height: 1.3; white-space: normal; word-break: break-word; }\n      .kh-day-cell.kh-blank { visibility: hidden; cursor: default; }\n      .kh-day-cell.status-blank { background: #ffffff; }\n      .kh-day-cell.status-error { background: #ffd6d6; border-color: #e05050; }\n      .kh-day-cell.status-done { background: #dff5df; border-color: #4caf50; }\n      .kh-day-cell.kh-leave { background: #cfe2ff; border-color: #4a90e2; }\n      .kh-day-cell.kh-holiday { color: #ff0000; font-weight: bold; }\n      .kh-day-cell.selected { outline: 3px solid #6633FF; font-weight: bold; }\n      .kh-cal-actions { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }\n      #kh-cal-count { font-weight: bold; color: #6633FF; }\n      .kh-section-list { display: grid; grid-template-columns: repeat(3, 1fr); gap: 6px 12px; margin: 8px 0; padding-top: 8px; border-top: 1px solid #ddd; }\n      .kh-section-list label { cursor: pointer; }\n      .kh-section-list .kh-section-note { grid-column: 1 / -1; }\n      .kh-section-actions { grid-column: 1 / -1; display: flex; align-items: center; gap: 8px; margin-top: 4px; }\n    `;document.head.appendChild(style)}function renderCalendarGrid(gridEl,year,month,buildCell){gridEl.innerHTML="";const weekdayNames=["日","月","火","水","木","金","土"];weekdayNames.forEach((wd,i)=>{const head=document.createElement("div");head.className="kh-cal-weekday-head"+(i===0||i===6?" kh-holiday":"");head.textContent=wd;gridEl.appendChild(head)});const firstWeekday=new Date(year,month-1,1).getDay();for(let i=0;i<firstWeekday;i++){const blank=document.createElement("div");blank.className="kh-day-cell kh-blank";gridEl.appendChild(blank)}buildMonthDays(year,month).forEach(d=>{gridEl.appendChild(buildCell(d))})}const DISCREPANCY_REASON_OPTIONS=[["0","指定なし"],["20","退社の身支度(クリアデスク、化粧直し等)"],["21","忘れ物を取りにきた、会社指示ではなく資料・PC等を置きに来た"],["22","仕事以外の待ち(雑談、待ち合わせ､仮眠、始発待ち等)"],["23","仕事以外の行事(懇親会等)"],["24","仕事以外の勉強会や自己啓発"],["30","PCの電源OFF操作(電源OFF忘れや、WindowsUpdate後の電源OFFなど)"],["31","業務に該当しないメールやカレンダー確認のためのPC操作（気になった、備忘・メモ、思い出し等の目的）"],["32","業務とは関係ない給与明細などの社内情報確認、保養所や個人的な申請のためのPC操作"],["40","その他(報告欄に詳細記載すること)"]];function ensureDiscrepancyReasonSelect(){const input=document.querySelector('input[name="RTTLVAL195_3_12"]');if(!input||input.dataset.khEnhanced)return;input.dataset.khEnhanced="1";const select=document.createElement("select");select.id="kh-discrepancy-reason-select";select.style.cssText="max-width:260px; font-size:14px;";DISCREPANCY_REASON_OPTIONS.forEach(([value,label])=>{const opt=document.createElement("option");opt.value=value;opt.textContent=`${value}: ${label}`;select.appendChild(opt)});const current=(input.value||"").trim()||"0";select.value=DISCREPANCY_REASON_OPTIONS.some(([v])=>v===current)?current:"0";select.addEventListener("change",()=>{input.value=select.value});input.style.display="none";input.after(select)}async function injectBulkUiIfNeeded(){if(document.getElementById("kh-bulk-box"))return;const current=getDisplayedYearMonthDay();const hdTable=document.getElementById("HD");if(!current||!hdTable)return;const originDateKey=`${current.year}_${current.month}_${current.day}`;const prevJob=await getJob();const restoreQueue=prevJob&&prevJob.failed&&prevJob.kind==="bulkCopy"&&prevJob.originDateKey===originDateKey?prevJob.queue:null;await chrome.storage.local.remove(PENDING_KEY);ensureBulkStylesInjected();const savedSections=(await chrome.storage.local.get(SECTION_PREFS_KEY))[SECTION_PREFS_KEY]||{};const sectionsHtml=SECTION_DEFS.map(({key:key,label:label})=>{const checked=savedSections[key]!==false?"checked":"";return`<label><input type="checkbox" class="kh-section-cb" data-section="${key}" ${checked}> ${label}</label>`}).join("\n");const box=document.createElement("div");box.id="kh-bulk-box";box.innerHTML=`\n      <div style="display:flex;justify-content:space-between;align-items:center;">\n        <label class="kh-checkbox-label"><input type="checkbox" id="kh-bulk-enable"> 他日へ一括入力する（この画面の入力内容を選んだ日にもコピーします）</label>\n        <button type="button" id="kh-bulk-help-btn" style="background:#f0edff;color:#6633FF;border:1px solid #6633FF;border-radius:4px;padding:6px 14px;font-weight:bold;cursor:pointer;font-size:14px;">ヘルプ</button>\n      </div>\n      <div id="kh-bulk-help-panel" style="display:none;background:#fff;border:1px solid #6633FF;border-radius:6px;padding:12px 14px;font-size:14px;line-height:1.8;margin:8px 0;">\n        <ul style="margin:0;padding-left:1.2em;list-style-type:disc;list-style-position:outside;">\n          <li style="margin-bottom:6px;list-style-type:disc;">カレンダー(未入力=白 / エラーあり=赤 / 入力済み=緑 / 休暇=青、土日祝は赤字)で、同じ内容を反映したい他の日をクリックして選択します(複数選択可)。</li>\n          <li style="margin-bottom:6px;list-style-type:disc;">「全選択(平日)」「全選択(勤務日)」「全選択(エラー日)」「全選択解除」でまとめて選択・解除できます。</li>\n          <li style="margin-bottom:6px;list-style-type:disc;">「コピーする項目」で、他日にコピーしたい項目だけチェックします(初期状態は前回設定を引き継ぎます)。</li>\n          <li style="margin-bottom:6px;list-style-type:disc;">この日(表示中の日)の入力はそのまま通常通り行い、「次へ」→確認画面→「申請」まで自分の手で進めてください。<br>「申請」を実際に押した場合のみ、選択した他日への自動反映が始まります。「月一覧へ」等で申請せずに一覧へ戻った場合はコピーされません。</li>\n          <li style="list-style-type:disc;">「申請」後、月一覧画面で自動的に他日への入力・申請が進みます。処理中は「処理を中断する」ボタンでいつでも中断できます。</li>\n        </ul>\n      </div>\n      <div id="kh-bulk-calendar" style="display:none">\n        <div class="kh-cal-note">この日の内容は「次へ」→確認画面→「申請」まで自分で進めてください。実際に「申請」を押したときのみ、下で選んだ他の日への自動反映が始まります。（「月一覧へ」等で申請せずに一覧へ戻った場合は反映されません）</div>\n        <div class="kh-cal-legend">\n          <span class="kh-legend-dot" style="background:#fff"></span>未入力\n          <span class="kh-legend-dot" style="background:#ffd6d6"></span>エラーあり\n          <span class="kh-legend-dot" style="background:#dff5df"></span>入力済み\n          <span class="kh-legend-dot" style="background:#cfe2ff"></span>休暇\n        </div>\n        <div id="kh-cal-grid" class="kh-cal-grid"></div>\n        <div class="kh-cal-actions">\n          <button type="button" id="kh-cal-weekday-select">全選択（平日）</button>\n          <button type="button" id="kh-cal-workday-select">全選択（勤務日）</button>\n          <button type="button" id="kh-cal-error-select">全選択（エラー日）</button>\n          <button type="button" id="kh-cal-clear">全選択解除</button>\n          <span id="kh-cal-count">0件選択中</span>\n        </div>\n        <div class="kh-section-list">\n          <div class="kh-cal-note kh-section-note">コピーする項目（チェックを外した項目は他日にコピーされません）</div>\n          ${sectionsHtml}\n          <div class="kh-section-actions">\n            <button type="button" id="kh-section-select-all">全選択</button>\n            <button type="button" id="kh-section-clear-all">全選択解除</button>\n          </div>\n        </div>\n      </div>\n    `;hdTable.parentNode.insertBefore(box,hdTable.nextSibling);document.getElementById("kh-bulk-help-btn").addEventListener("click",()=>{const helpPanel=document.getElementById("kh-bulk-help-panel");helpPanel.style.display=helpPanel.style.display==="none"?"block":"none"});const enableCb=document.getElementById("kh-bulk-enable");const calDiv=document.getElementById("kh-bulk-calendar");const grid=document.getElementById("kh-cal-grid");const countEl=document.getElementById("kh-cal-count");const selected=new Set(restoreQueue||[]);const monthStatus=await getCachedMonthStatus(current.year,current.month);function renderGrid(){renderCalendarGrid(grid,current.year,current.month,d=>{const isCurrent=d.day===current.day;const info=monthStatus[String(d.day)];const status=info?info.status:"blank";const isHoliday=d.isHoliday||info&&info.isHoliday;const isLeave=info&&info.isLeave;const cell=document.createElement("div");cell.className="kh-day-cell status-"+status+(isLeave?" kh-leave":"")+(isHoliday?" kh-holiday":"")+(isCurrent?"":selected.has(d.key)?" selected":"");cell.textContent=`${d.day}`+(isCurrent?" [この日]":"");if(!isCurrent){cell.addEventListener("click",()=>{if(selected.has(d.key))selected.delete(d.key);else selected.add(d.key);renderGrid();stageNow()})}return cell});countEl.textContent=`${selected.size}件選択中`}let stagingTimer=null;function readSectionFlags(){const flags={};document.querySelectorAll(".kh-section-cb").forEach(cb=>{flags[cb.dataset.section]=cb.checked});return flags}function stageNow(){if(!enableCb.checked||selected.size===0)return;const others=Array.from(selected).sort((a,b)=>Number(a.split("_")[2])-Number(b.split("_")[2]));chrome.storage.local.set({[PENDING_KEY]:{originDateKey:originDateKey,queue:others,formData:captureCurrentFormData(),sections:readSectionFlags()}})}function startStaging(){stageNow();if(!stagingTimer)stagingTimer=setInterval(stageNow,1e3)}function stopStaging(){chrome.storage.local.remove(PENDING_KEY);if(stagingTimer){clearInterval(stagingTimer);stagingTimer=null}}if(restoreQueue&&restoreQueue.length){enableCb.checked=true;calDiv.style.display="block";startStaging()}enableCb.addEventListener("change",()=>{calDiv.style.display=enableCb.checked?"block":"none";if(enableCb.checked)startStaging();else stopStaging()});document.getElementById("kh-cal-weekday-select").addEventListener("click",()=>{selected.clear();buildMonthDays(current.year,current.month).forEach(d=>{if(d.day===current.day)return;const info=monthStatus[String(d.day)];const isHoliday=d.isHoliday||info&&info.isHoliday;if(!isHoliday)selected.add(d.key)});renderGrid();stageNow()});document.getElementById("kh-cal-workday-select").addEventListener("click",()=>{selected.clear();buildMonthDays(current.year,current.month).forEach(d=>{if(d.day===current.day)return;const info=monthStatus[String(d.day)];const isHoliday=d.isHoliday||info&&info.isHoliday;const isLeave=info&&info.isLeave;if(!isHoliday&&!isLeave)selected.add(d.key)});renderGrid();stageNow()});document.getElementById("kh-cal-error-select").addEventListener("click",()=>{selected.clear();buildMonthDays(current.year,current.month).forEach(d=>{if(d.day===current.day)return;const info=monthStatus[String(d.day)];const status=info?info.status:"blank";if(status==="error")selected.add(d.key)});renderGrid();stageNow()});document.getElementById("kh-cal-clear").addEventListener("click",()=>{selected.clear();renderGrid();stopStaging()});document.querySelectorAll(".kh-section-cb").forEach(cb=>{cb.addEventListener("change",()=>{chrome.storage.local.set({[SECTION_PREFS_KEY]:readSectionFlags()});stageNow()})});function setAllSections(checked){document.querySelectorAll(".kh-section-cb").forEach(cb=>{cb.checked=checked});chrome.storage.local.set({[SECTION_PREFS_KEY]:readSectionFlags()});stageNow()}document.getElementById("kh-section-select-all").addEventListener("click",()=>setAllSections(true));document.getElementById("kh-section-clear-all").addEventListener("click",()=>setAllSections(false));renderGrid()}async function markPendingConfirmedOnSubmit(){const data=await chrome.storage.local.get(PENDING_KEY);if(!data[PENDING_KEY])return;const btn=document.querySelector('[id^="dSubmission"]');if(!btn)return;let resubmitting=false;btn.addEventListener("click",e=>{if(resubmitting)return;e.preventDefault();e.stopImmediatePropagation();(async()=>{const latest=await chrome.storage.local.get(PENDING_KEY);const pending=latest[PENDING_KEY];if(pending){pending.confirmed=true;await chrome.storage.local.set({[PENDING_KEY]:pending})}resubmitting=true;btn.click()})()},true)}async function injectReadOnlyCalendarIfPending(){if(document.getElementById("kh-bulk-box"))return;const data=await chrome.storage.local.get(PENDING_KEY);const pending=data[PENDING_KEY];const current=getDisplayedYearMonthDay();const hdTable=document.getElementById("HD");if(!pending||!pending.queue||!pending.queue.length||!current||!hdTable)return;ensureBulkStylesInjected();const monthStatus=await getCachedMonthStatus(current.year,current.month);const selectedSet=new Set(pending.queue);const sections=pending.sections||{};const sectionsHtml=SECTION_DEFS.map(({key:key,label:label})=>{const on=sections[key]!==false;return`<div>${on?"✅":"✕"} ${label}</div>`}).join("");const box=document.createElement("div");box.id="kh-bulk-box";box.innerHTML=`\n      <div class="kh-checkbox-label">他日へ一括入力: 以下の${pending.queue.length}件にも同じ内容が自動反映されます（この画面では変更できません）</div>\n      <div id="kh-cal-grid" class="kh-cal-grid"></div>\n      <div class="kh-section-list">\n        <div class="kh-cal-note kh-section-note">コピーする項目</div>\n        ${sectionsHtml}\n      </div>\n    `;hdTable.parentNode.insertBefore(box,hdTable.nextSibling);const grid=box.querySelector("#kh-cal-grid");renderCalendarGrid(grid,current.year,current.month,d=>{const info=monthStatus[String(d.day)];const status=info?info.status:"blank";const isHoliday=d.isHoliday||info&&info.isHoliday;const isLeave=info&&info.isLeave;const cell=document.createElement("div");cell.className="kh-day-cell status-"+status+(isLeave?" kh-leave":"")+(isHoliday?" kh-holiday":"")+(selectedSet.has(d.key)?" selected":"");cell.textContent=`${d.day}`;return cell})}const manifestInfo=chrome.runtime.getManifest();const TOOL_COMPANY_NAME=manifestInfo.companyName||"FSI";const TOOL_COMPANY_CONFIG_NAME=manifestInfo.companyConfigName||`${TOOL_COMPANY_NAME}-管理`;const TOOL_COMPANY_USAGE_NAME=manifestInfo.companyUsageName||`${TOOL_COMPANY_NAME}-利用者`;const TOOL_WEBAPP_URL="https://script.google.com/macros/s/AKfycbz2Y8IIiRaR3iIJ3YH9TXWrjvvG_wlcaziHYOasvAtqvjJXP57Cpcw1LnC6w9GzfzKuew/exec";const REMOTE_CONFIG_CACHE_KEY="kintaiRemoteConfigCache";const USAGE_LAST_REPORTED_KEY="kintaiUsageLastReportedDate";const FIRST_RUN_KEY="kintaiFirstRunDate";const GRACE_PERIOD_MS=30*24*60*60*1e3;const ADMIN_MESSAGE_DISMISS_KEY="kintaiAdminMessageDismissedInfo";const SURVEY_DISMISS_KEY="kintaiSurveyDismissedInfo";async function getOrSetFirstRunDate(){const data=await chrome.storage.local.get(FIRST_RUN_KEY);if(data[FIRST_RUN_KEY])return new Date(data[FIRST_RUN_KEY]);const now=new Date;await chrome.storage.local.set({[FIRST_RUN_KEY]:now.toISOString()});return now}async function sha256Hex(text){const data=(new TextEncoder).encode(text);const buf=await crypto.subtle.digest("SHA-256",data);return Array.from(new Uint8Array(buf)).map(b=>b.toString(16).padStart(2,"0")).join("")}function getEmployeeNumber(){const m=document.body.textContent.match(/社員番号[:：]\s*([0-9]+)/);return m?m[1]:null}function getDepartment(){const el=document.querySelector(".syznm");if(!el)return null;const t=el.textContent.replace(/^\s*所属[:：]\s*/,"").trim();return t||null}function parseJapaneseDate(str){if(!str)return null;const native=new Date(str);if(!Number.isNaN(native.getTime())){return new Date(native.getTime()+24*60*60*1e3-1)}const m=String(str).match(/(\d{4})\/(\d{1,2})\/(\d{1,2})/);if(!m)return null;return new Date(Number(m[1]),Number(m[2])-1,Number(m[3]),23,59,59,999)}async function fetchRemoteConfig(){try{const controller=new AbortController;const timeoutId=setTimeout(()=>controller.abort(),5e3);const res=await fetch(`${TOOL_WEBAPP_URL}?company=${encodeURIComponent(TOOL_COMPANY_NAME)}&config=${encodeURIComponent(TOOL_COMPANY_CONFIG_NAME)}`,{signal:controller.signal});clearTimeout(timeoutId);if(!res.ok)throw new Error(`HTTP ${res.status}`);const config=await res.json();await chrome.storage.local.set({[REMOTE_CONFIG_CACHE_KEY]:{config:config,checkedAt:Date.now()}});return config}catch(e){const cached=(await chrome.storage.local.get(REMOTE_CONFIG_CACHE_KEY))[REMOTE_CONFIG_CACHE_KEY];return cached?cached.config:null}}function evaluateRemoteConfig(config){if(!config)return{disabled:false};if(config["稼働状況"]&&String(config["稼働状況"]).trim().toUpperCase()==="OFF"){return{disabled:true,reason:"管理者により本ツールの利用が停止されています。",reasonType:"off"}}const expireDate=parseJapaneseDate(config["利用期限"]);if(expireDate&&new Date>expireDate){return{disabled:true,reason:`利用期限(${expireDate.toLocaleDateString("ja-JP",{timeZone:"Asia/Tokyo"})})を過ぎているため停止されています。管理者にご確認ください。`,reasonType:"expired"}}return{disabled:false}}function showDisabledPopup(reason){if(document.getElementById("kh-disabled-overlay"))return;document.body.appendChild(buildWarningOverlay({baseId:"kh-disabled",zIndex:2147483647,color:"#c0392b",title:"⛔ ツールが利用できません",message:reason}))}function showExpirationWarningPopup(expireDate){if(document.getElementById("kh-expiration-warning-overlay"))return;document.body.appendChild(buildWarningOverlay({baseId:"kh-expiration-warning",zIndex:2147483645,color:"#e6a817",title:"⚠ 利用期限が近づいています",message:`利用期限(${expireDate.toLocaleDateString("ja-JP",{timeZone:"Asia/Tokyo"})})まで残り3か月を切っています。必要に応じて管理者に延長をご確認ください。`}))}function showUpdateAvailableBanner(remoteVersion,updateUrl){if(document.getElementById("kh-update-overlay"))return;const overlay=buildWarningOverlay({baseId:"kh-update",zIndex:2147483646,color:"#2f6fed",title:"🆕 新しいバージョンがあります",message:`<div style="display:flex;justify-content:flex-end;margin-bottom:8px;"><button type="button" id="kh-update-help-btn" style="background:#eaf1ff;color:#2f6fed;border:1px solid #2f6fed;border-radius:4px;padding:4px 12px;font-weight:bold;cursor:pointer;font-size:13px;">ヘルプ</button></div><div id="kh-update-help-panel" style="display:none;background:#f0f5ff;border:1px solid #2f6fed;border-radius:6px;padding:10px 12px;margin-bottom:10px;font-size:13px;line-height:1.7;"><ol style="margin:0;padding-left:1.2em;list-style-type:decimal;list-style-position:outside;"><li style="margin-bottom:4px;">アドレスバーに以下を入力してEnter<ul style="margin:4px 0;padding-left:1.2em;list-style-type:disc;"><li>Edgeの場合: edge://extensions/</li><li>Chromeの場合: chrome://extensions/</li></ul></li><li style="margin-bottom:4px;">「CWS勤怠自動化ツール」の拡張機能カードにある「削除」ボタンをクリックして削除する</li><li style="margin-bottom:4px;">旧バージョンの拡張機能を削除する</li><li style="margin-bottom:4px;">新しいZIPファイルをPC内の好きな場所に展開(解凍)する</li><li>手順1の拡張機能の一覧画面で「開発者モード」がONになっていることを確認し、「展開して読み込み」(Load unpacked)ボタンをクリックして、手順4で展開した新しいフォルダを選択する</li></ol></div>最新のバージョン(Ver.${escapeHtml(String(remoteVersion))})が公開されています。<br>最新バージョンをインストールしてください。`,applyLabel:updateUrl?"リンクを開く":"",onApply:updateUrl?()=>{window.open(updateUrl,"_blank")}:undefined});overlay.querySelector("#kh-update-help-btn").addEventListener("click",()=>{const helpPanel=overlay.querySelector("#kh-update-help-panel");helpPanel.style.display=helpPanel.style.display==="none"?"block":"none"});document.body.appendChild(overlay)}function linkifyEscapedText(text){return text.replace(/(https?:\/\/[^\s<]+)/g,'<a href="$1" target="_blank" rel="noopener noreferrer" style="color:inherit;text-decoration:underline;">$1</a>')}function showAdminMessagePopup(message,dismissDays){if(document.getElementById("kh-admin-message-overlay"))return;document.body.appendChild(buildWarningOverlay({baseId:"kh-admin-message",zIndex:2147483644,color:"#16a085",title:"📢 管理者からのお知らせ",message:linkifyEscapedText(escapeHtml(String(message))).replace(/\r\n|\r|\n/g,"<br>"),applyLabel:`${dismissDays}日間は表示しない`,onApply:async()=>{await chrome.storage.local.set({[ADMIN_MESSAGE_DISMISS_KEY]:{dismissedAt:Date.now(),message:message}})}}))}function showSurveyPopup(surveyUrl,dismissDays){if(document.getElementById("kh-survey-overlay"))return;const overlay=buildWarningOverlay({baseId:"kh-survey",zIndex:2147483644,color:"#16a085",title:"📝 アンケートのご協力をお願いします",message:`より良いツールにするため、<br>アンケートにご協力ください。<br><br><a id="kh-survey-link" href="${escapeHtml(surveyUrl)}" target="_blank" rel="noopener noreferrer" style="color:inherit;text-decoration:underline;">${escapeHtml(surveyUrl)}</a>`,applyLabel:`${dismissDays}日間は再表示しない`,onApply:async()=>{await chrome.storage.local.set({[SURVEY_DISMISS_KEY]:{dismissedAt:Date.now(),url:surveyUrl}})}});overlay.querySelector("#kh-survey-link").addEventListener("click",async(e)=>{e.preventDefault();await chrome.storage.local.set({[SURVEY_DISMISS_KEY]:{permanent:true,url:surveyUrl}});window.open(surveyUrl,"_blank","noopener,noreferrer")});document.body.appendChild(overlay)}async function reportUsageIfNeeded(){try{const jstNow=new Date(Date.now()+9*60*60*1e3);const yearMonth=`${jstNow.getUTCFullYear()}-${String(jstNow.getUTCMonth()+1).padStart(2,"0")}`;const data=await chrome.storage.local.get(USAGE_LAST_REPORTED_KEY);if(data[USAGE_LAST_REPORTED_KEY]===yearMonth)return;const empNo=getEmployeeNumber();if(!empNo)return;const userHash=await sha256Hex(empNo);const controller=new AbortController;const timeoutId=setTimeout(()=>controller.abort(),5e3);const res=await fetch(TOOL_WEBAPP_URL,{method:"POST",body:JSON.stringify({companyName:TOOL_COMPANY_NAME,usageName:TOOL_COMPANY_USAGE_NAME,userHash:userHash,yearMonth:yearMonth,department:getDepartment()}),signal:controller.signal});clearTimeout(timeoutId);if(!res.ok)return;const result=await res.json();if(!result||result.ok!==true)return;await chrome.storage.local.set({[USAGE_LAST_REPORTED_KEY]:yearMonth})}catch(e){}}async function checkRemoteStatusForList(){let disabledInfo=null;try{const remoteConfig=await fetchRemoteConfig();const remoteStatus=evaluateRemoteConfig(remoteConfig);if(remoteStatus.disabled){disabledInfo=remoteStatus;if(disabledInfo.reasonType==="expired"){const firstRunDate=await getOrSetFirstRunDate();if(Date.now()-firstRunDate.getTime()<GRACE_PERIOD_MS){disabledInfo=null}}}else{if(remoteConfig&&remoteConfig["version"]){const localVersion=parseFloat(manifestInfo.version);const remoteVersion=parseFloat(remoteConfig["version"]);if(!Number.isNaN(remoteVersion)&&remoteVersion>localVersion){showUpdateAvailableBanner(remoteConfig["version"],remoteConfig["新バージョンのダウンロード場所"])}}const expireDate=parseJapaneseDate(remoteConfig&&remoteConfig["利用期限"]);if(expireDate){const warnFromDate=new Date(expireDate);warnFromDate.setMonth(warnFromDate.getMonth()-3);if(new Date>=warnFromDate)showExpirationWarningPopup(expireDate)}const adminMessage=remoteConfig&&remoteConfig["管理者からのメッセージ"];if(adminMessage&&String(adminMessage).trim()!==""){const trimmedAdminMessage=String(adminMessage).trim();const periodStartRaw=remoteConfig["メッセージの表示期間（開始）"];const periodStartDate=periodStartRaw?new Date(periodStartRaw):null;const periodStart=periodStartDate&&!isNaN(periodStartDate.getTime())?periodStartDate:null;const periodEnd=parseJapaneseDate(remoteConfig["メッセージの表示期間（終了）"]);const nowForPeriod=new Date;const withinPeriod=(!periodStart||nowForPeriod>=periodStart)&&(!periodEnd||nowForPeriod<=periodEnd);if(withinPeriod){const dismissDaysRaw=remoteConfig["メッセージの停止可能日数"];const dismissDays=typeof dismissDaysRaw==="number"&&dismissDaysRaw>0?dismissDaysRaw:3;const dismissData=(await chrome.storage.local.get(ADMIN_MESSAGE_DISMISS_KEY))[ADMIN_MESSAGE_DISMISS_KEY];const dismissStillValid=dismissData&&dismissData.message===trimmedAdminMessage&&Date.now()-dismissData.dismissedAt<dismissDays*24*60*60*1e3;if(!dismissStillValid){showAdminMessagePopup(trimmedAdminMessage,dismissDays)}}}const surveyUrl=remoteConfig&&remoteConfig["アンケート依頼"];if(surveyUrl&&String(surveyUrl).trim()!==""){const trimmedSurveyUrl=String(surveyUrl).trim();const surveyPeriodStartRaw=remoteConfig["アンケートの表示期間（開始）"];const surveyPeriodStartDate=surveyPeriodStartRaw?new Date(surveyPeriodStartRaw):null;const surveyPeriodStart=surveyPeriodStartDate&&!isNaN(surveyPeriodStartDate.getTime())?surveyPeriodStartDate:null;const surveyPeriodEnd=parseJapaneseDate(remoteConfig["アンケートの表示期間（終了）"]);const nowForSurveyPeriod=new Date;const withinSurveyPeriod=(!surveyPeriodStart||nowForSurveyPeriod>=surveyPeriodStart)&&(!surveyPeriodEnd||nowForSurveyPeriod<=surveyPeriodEnd);if(withinSurveyPeriod){const surveyDismissDaysRaw=remoteConfig["アンケートの停止可能日数"];const surveyDismissDays=typeof surveyDismissDaysRaw==="number"&&surveyDismissDaysRaw>0?surveyDismissDaysRaw:3;const surveyDismissData=(await chrome.storage.local.get(SURVEY_DISMISS_KEY))[SURVEY_DISMISS_KEY];const surveyDismissStillValid=surveyDismissData&&surveyDismissData.url===trimmedSurveyUrl&&(surveyDismissData.permanent||Date.now()-surveyDismissData.dismissedAt<surveyDismissDays*24*60*60*1e3);if(!surveyDismissStillValid){showSurveyPopup(trimmedSurveyUrl,surveyDismissDays)}}}reportUsageIfNeeded()}}catch(e){}return disabledInfo}(async()=>{const pageType=detectPageType();await ensureFreshSchema();const job=await getJob();const remoteCheckPromise=pageType==="list"&&(!job||!job.active)?checkRemoteStatusForList():null;if(pageType==="list"&&(!job||!job.active)){await promotePendingIfReturnedToList()}if(pageType==="detail"&&(!job||!job.active)){ensureDiscrepancyReasonSelect();await injectBulkUiIfNeeded()}if(pageType==="confirm"&&(!job||!job.active)){await injectReadOnlyCalendarIfPending();await markPendingConfirmedOnSubmit()}await updateStatusPanel();document.getElementById("kh-early-overlay")?.remove();await processIfJobActive();await updateStatusPanel();if(pageType==="list"){await scrapeAndCacheMonthStatus()}if(remoteCheckPromise){const disabledInfo=await remoteCheckPromise;if(disabledInfo){showDisabledPopup(disabledInfo.reason);return}}if(pageType==="list"){const jobFinal=await getJob();if(!jobFinal||!jobFinal.active){await injectAutoFillButtonIfNeeded();await maybeShowUnappliedLeaveWarning();await maybeShowUnappliedWorkdayWarning();await maybeShowLongExecWithoutReportWarning();await maybeShowDiscrepancy40WithoutReportWarning();await maybeShowTimeDeviationWarning()}}})()})();